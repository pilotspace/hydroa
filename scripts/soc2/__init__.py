"""SOC 2 repo-governance tooling (soc2-groundwork milestone).

These are ORG-internal change-management / control-evidence tools that run against the
repository and its GitHub metadata. They are deliberately NOT part of the gateway runtime
wheel — nothing here imports `gateway.*`.
"""
