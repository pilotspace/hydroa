# WAVE-3 shared context — file-search-tool direction drafting (2026-07-24)

Orchestrator-owned. Read FIRST. Do NOT edit this file, MILESTONE.md, state.json, or any other task's files.

## Mission
Milestone `managed-rag-finetune` (R5, feeds 0.13.0). Waves 1+2 are DONE + gated PASS — their §3
contracts are FROZEN and their code is committed on branch `feat/managed-rag-finetune`. Wave-3 is the
FINAL task: `file-search-tool`. Full scope + Ground: `.add/milestones/managed-rag-finetune/MILESTONE.md`.

## Your beat: DIRECTION only (draft the whole bundle; do NOT freeze)
- Phase guide: `/Users/tindang/.claude/skills/add/phases/direction.md` (read it; §1 → §3 → §4).
- Draft the WHOLE bundle in ONE write into `.add/tasks/file-search-tool/PLAN.md` (scaffolded there).
- Write the §4 red suite as REAL tests under `apps/gateway/tests/file_search_tool/` and RUN it — it must
  fail for the RIGHT reason (missing implementation), evidence pasted into §4. Use
  GATEWAY_TEST_DATABASE_URL=postgresql+asyncpg://gateway:gateway@localhost:5433/gateway_test_file_search_tool
  (CREATE the DB first: `docker exec hydroa-dev-postgres-1 psql -U gateway -d postgres -c "CREATE DATABASE gateway_test_file_search_tool"`;
  the pgvector `vector` column means the test DB needs `CREATE EXTENSION IF NOT EXISTS vector` — conftest
  provisions it on the base/worker DBs, but a manually-created DB may need it too).
  Run pytest with `--override-ini="addopts="` to skip the repo coverage gate. macOS has NO `timeout`.
- Leave §3 `Status: DRAFT`. NEVER run `add.py freeze | advance | gate | use`.
- End with the freeze card (banner → ARC → SHAPE → SUMMARY → FLAGS lowest-confidence-first → DECIDED
  → EVIDENCE → APPROVE → NEXT) as your final report.

## What this task ships (the goal)
Expose OpenAI's `file_search` tool so an SDK app can attach a `vector_store_id` and have the gateway
retrieve top-k chunks and ground the model's answer — in BOTH `/v1/responses` AND chat completions —
and METER each retrieval as exactly ONE new `per_query` billable unit.

## The FROZEN contracts you build on (do NOT re-freeze; consume the named extension points)
- **vector-store-core + vector-store-files §3 (FROZEN):** tables `vector_stores`, `vector_store_files`
  (`status` column), `vector_store_chunks` (`embedding vector(1536) NOT NULL`, HNSW cosine index,
  `tenant_id` denormalized, `store_id`, chunk text/metadata). Module `apps/gateway/src/gateway/vector_stores/`.
  **Extension point (wave-2 designed for you):** top-k retrieval queries `vector_store_chunks` with ZERO
  DDL / ZERO re-freeze. `VectorStoreFileRepository` (infrastructure/file_repository.py) has NO search
  method yet — ADD a top-k cosine query there (or a sibling read-repository) using the HNSW index. Filter
  by tenant_id AND store_id; cross-tenant store → 404 (never a leak). Per-store `embedding_model`/`embedding_dim`
  columns exist — retrieval MUST embed the query with the SAME model the store was built with.
- **Billing dispatcher (FROZEN mechanism):** `usage/application/recorder.py` — `_known_units` set appears
  TWICE (~line 372 main path, ~line 490 cache-hit path): currently `{per_token, per_image, per_second,
  per_tool_call, ...}`. Add `"per_query"` additively to BOTH (the `per_tool_call` precedent). Price via the
  ONE shared rate-card resolver; the `quantity` for file_search = number of retrievals (1 per file_search
  invocation, NOT per chunk). Exactly ONE usage record per billable retrieval. Snapshot must carry a
  `per_query` rate for the served/tool model — reuse the pricing_snapshot mechanism, no new billing plumbing.
- **/v1/responses ingress (FROZEN @ v1):** `proxy/api/responses_router.py::responses()` (line 178) calls
  `responses_request_to_chat(raw_body)` → chat pipeline → `chat_response_to_responses`. The `tools` array
  (incl. `{"type":"file_search","vector_store_ids":[...]}`) rides in the request. Translation seam:
  `proxy/infrastructure/openai_responses_ingress.py`. Design so a request with NO file_search tool engages
  ZERO new plumbing (byte-identical default path).
- **Chat completions:** `proxy/api/router.py` — same tool must work here too (OpenAI puts file_search in
  the chat `tools` array as well). Name the ONE shared retrieval+grounding seam both ingresses call.

## Hard project rules (non-negotiable)
- GROUND by live reads (serena MCP preferred), never memory; cite SYMBOLS; tag [OBSERVED]/[DERIVED]/[PRIOR]/[ASSUMED].
- Byte-identical default path: a request not using file_search engages ZERO new plumbing.
- No outbound IO without timeout + bounded retry (idempotent only) + circuit breaker — the query-embedding
  call reuses vector-store-files' PER-TENANT breaker (this repo HARD-STOPPED on a shared breaker twice).
- Tenant scoping on every row; cross-tenant vector_store_id → 404 (never a leak, never 403/404 alternation).
- ZDR interplay: retrieval READS existing chunks (already ZDR-gated at write). The user's QUERY text sent
  to the embedder + any retrieved chunk text echoed into a stored Response must honor the tenant's ZDR
  obligation — if the response is persisted (stored_responses), retrieved chunk text at-rest is payload;
  re-evaluate `raise_if_zdr` on the persist path (see [[zdr-toctou-async-write-paths]] meta-pattern).
- New `per_query` unit goes wherever pricing units are enumerated (recorder `_known_units` x2, any catalog
  seed/validation, the flusher passthrough already generic on the string). NO test weakened, NO frozen
  contract edited. Chain any migration to the CURRENT head `b3d8f21ca9e6`.
- Pyright strict; TypedDict/typed extras seam — no inspect.signature dispatch.

## Key anchors (verify live before citing)
- Chunk store + retrieval: `apps/gateway/src/gateway/vector_stores/infrastructure/{orm.py,file_repository.py}`
  (`VectorStoreChunkRow`, `VectorStoreFileRepository`).
- Query embedding: vector-store-files `infrastructure/embedding_client.py` (per-tenant breaker) — reuse it.
- Billing: `usage/application/recorder.py` (`_known_units` x2), `usage/infrastructure/orm.py` (pricing_unit col),
  `usage/application/flusher.py` (generic passthrough).
- /v1/responses: `proxy/api/responses_router.py`, `proxy/infrastructure/openai_responses_ingress.py`.
- Chat: `proxy/api/router.py`.
- Catalog rate-card resolver + pricing_snapshots: `apps/gateway/src/gateway/catalog/`.

## Report discipline
Self-score (6 dims, refine weakest <0.9 before reporting). Final message = the freeze card only.
Ground everything in LIVE reads; do NOT trust this file's line numbers without re-confirming via serena.
