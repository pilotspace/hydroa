# Carried todos — ADD 2.x → GitHub issues

The 2.x bundle carried an inline `todos` list inside `state.json`. ADD 3.x has no inline
todo store, so on 2026-08-12 the **78 open** todos were exported to GitHub issues and the
**45 done** ones were left in the archived `state.json` as closed history. GitHub issue
numbers share one sequence with PRs, so an ADD todo id could not be reused as its issue
number — each issue title is prefixed `[add-todo #N]` and its body records the original
id, full text, and creation date. This file is the id → issue-URL map.

Exported: 78 open todos → issues #121–#198. Done todos not exported: 45 (see `state.json`).

| add-todo # | created | issue | text |
|---:|---|---|---|
| 1 | 2026-06-28 | [#121](https://github.com/pilotspace/hydroa/issues/121) | fix sidebar full height |
| 2 | 2026-06-28 | [#122](https://github.com/pilotspace/hydroa/issues/122) | paging model catalog with fuzzy search |
| 3 | 2026-06-28 | [#123](https://github.com/pilotspace/hydroa/issues/123) | upgrade UI of each page |
| 4 | 2026-06-30 | [#124](https://github.com/pilotspace/hydroa/issues/124) | PROGRAM 'AI feature depth (Console-grade)' roadmap after voice: m3 memory-playground · m4 artifacts-playground · m5 vision-playground · m6 video-playground · m7 proxy-correctness-vs-docs (audit every gateway adapter against provider API docs). Create each just-in-time. |
| 5 | 2026-07-06 | [#125](https://github.com/pilotspace/hydroa/issues/125) | DialogContent (components/ui/dialog.tsx) has no entrance/exit animation of its own -- only DialogOverlay fades. Every Dialog consumer (AppShell mobile nav sheet, command-palette) snaps the panel open/closed. Existing motion system (Reveal in motion.tsx + the global reduced-motion net in globals.css) already establishes the right pattern (motion-safe: opt-in, unconditional render, global reduced-motion backstop) -- extending DialogContent to match is a good small follow-up. Also worth revisiting: tenant-overview-strip's 4 tiles currently just appear as each query resolves, no progressive-reveal of their own. Raised by Tin 2026-07-06; deferred rather than touching the shared dialog.tsx primitive mid-build of command-palette. |
| 6 | 2026-07-12 | [#126](https://github.com/pilotspace/hydroa/issues/126) | margin-dashboard follow-on (P2, Tin approved 2026-07-12): clearly-labeled 'estimated margin (catalog list-price basis)' column — separate task, never blended with authoritative provider-cost margin |
| 7 | 2026-07-12 | [#127](https://github.com/pilotspace/hydroa/issues/127) | billing-ui F1 (verify 🟡, spec delta seeded): Plan&seats live roster count 403-degrades to '—' for operator/billing_admin/viewer/member — decide (a) ungated tenant-self count-only read vs (b) document degrade as accepted; Tin design call at observe/M-close |
| 8 | 2026-07-12 | [#128](https://github.com/pilotspace/hydroa/issues/128) | billing-ui F2 (💭): evidence-drawer per-line context subtitle from DESIGN.md dropped in build — cosmetic fast-follow or backlog |
| 9 | 2026-07-12 | [#129](https://github.com/pilotspace/hydroa/issues/129) | plan-seat-cap seeded delta: SCIM PATCH active:true reactivation bypasses seat cap (confirmed repro in test_verify_adversarial) — gate reactivation via assert_seat_available in a follow-on task |
| 10 | 2026-07-12 | [#130](https://github.com/pilotspace/hydroa/issues/130) | margin-dashboard delta: /summary has_provider_cost_data uses a heuristic, not by-tenant-model/trend's honest COUNT(cost_basis='provider') — align in follow-on |
| 14 | 2026-07-12 | [#131](https://github.com/pilotspace/hydroa/issues/131) | seat-billing observe: record §2 prose correction (reactivation-gap arithmetic 24 -> 16 per M6, implemented+tested as 16, disclosed) + decide narrowing join_verified_tenant_domain broad IntegrityError catch (defect-masking, left additive-safe) |
| 15 | 2026-07-12 | [#132](https://github.com/pilotspace/hydroa/issues/132) | seat-billing delta F1: contract prose says seat prices tenant-overridable via rate-card resolver — no such code path (_load_seat_price reads plans only). Tin: build override or fix prose |
| 16 | 2026-07-12 | [#133](https://github.com/pilotspace/hydroa/issues/133) | seat-billing delta F2/F3: document seat-evidence live-re-derivation drift vs frozen totals + add DB append-only trigger to seat_membership_events (mirror invoice_lines trigger) |
| 17 | 2026-07-12 | [#134](https://github.com/pilotspace/hydroa/issues/134) | seat-billing delta F5 (cutover caveat): pre-migration deactivate->reactivate cycles invisible to backfill — first post-cutover invoice can over-bill (5-day demo in probes). Tin: accept as documented Non-goal or manual reconciliation pass |
| 18 | 2026-07-12 | [#135](https://github.com/pilotspace/hydroa/issues/135) | region-catalog residue: add coded id-namespace guard — dynamic-sourced CatalogModel.id must never collide with static-seed prefixes (us./eu./apac.); safety today rests on incidental OpenRouter slash-format + composite processing order (verify 2026-07-12) |
| 19 | 2026-07-12 | [#136](https://github.com/pilotspace/hydroa/issues/136) | region-catalog docs: §3 DDL prose still shows 3-value VALID_REGIONS literal; DECIDED addendum (4-value incl ap) is authoritative — fold to one canonical statement next contract touch |
| 20 | 2026-07-12 | [#137](https://github.com/pilotspace/hydroa/issues/137) | region-pricing SPEC: DELETE /admin/region-pricing/{region} should case-normalize region before matching — a typo'd DELETE (EU vs eu) returns 204 but silently leaves the override active, still billing the old rate (verify live-probed 2026-07-12) |
| 21 | 2026-07-12 | [#138](https://github.com/pilotspace/hydroa/issues/138) | region-pricing SPEC: flag 'global' as a high-blast-radius override target in admin docs/UI — one PUT .../global reprices EVERY default-region model for that tenant (global is the model default region) |
| 22 | 2026-07-12 | [#139](https://github.com/pilotspace/hydroa/issues/139) | region-pricing TDD: add a direct parity assertion between rate_card_resolver._REGION_MULTIPLIER_SEEDS and catalog/repository.py's CASE literal (hand-synced seed drift currently caught only by one integration test, eu-only) |
| 23 | 2026-07-12 | [#140](https://github.com/pilotspace/hydroa/issues/140) | vertex-adapter SPEC (defense-in-depth): harden all *_ad.py token providers' httpx except-clauses to catch httpx.RequestError broadly (not just Timeout\|NetworkError) — ProtocolError/ProxyError propagate unhandled, retaining signed assertion in __context__; systemic pattern shared with azure_ad.py |
| 24 | 2026-07-12 | [#141](https://github.com/pilotspace/hydroa/issues/141) | vertex-adapter SPEC (defense-in-depth): validate GoogleServiceAccountCredential.project_id against GCP project-id charset before _build_url f-string interpolation — non-exploitable for host-hijack today (host is fixed literal) but worth tightening |
| 25 | 2026-07-12 | [#142](https://github.com/pilotspace/hydroa/issues/142) | vertex-adapter: before shipping, optionally run scripts/live_vertex_verify.py against a real GCP service account (manual smoke, outside automated gate — no live creds were available at build/verify time) |
| 26 | 2026-07-13 | [#143](https://github.com/pilotspace/hydroa/issues/143) | ADD anti-pattern (verify): when re-running a security exploit as an independent probe, model the adversary's dependency on the SECRET (private_key/client_secret), not just the public identity — a fake IDP/factory that ignores the secret can manufacture a false-positive HARD-STOP (self-caught in vertex re-verify 2026-07-13) |
| 27 | 2026-07-13 | [#144](https://github.com/pilotspace/hydroa/issues/144) | SECURITY (M2 EU-pin gap, Bedrock BYOK): BedrockCredential.region (tenant-supplied via PUT /admin/provider-keys/bedrock) drives the physical dial endpoint bedrock-runtime.{region}.amazonaws.com, DECOUPLED from the catalog region tag residency-policy filters on. An EU-pinned tenant with a us-east-1 Bedrock cred has its PROMPT sent to a US endpoint (payload transit = the breach) even if AWS then rejects the eu.-profile invocation. Vertex avoided this (fixed prefix→location map). Options: (a) pre-dial structural guard validating cred region vs candidate catalog-region prefix [RECOMMENDED, mirrors vertex], (b) live-AWS ValidationException test, (c) signed RISK-ACCEPTED. ESCALATED to Tin 2026-07-13 — do NOT treat M10/EU-pin as closed for Bedrock until resolved. |
| 28 | 2026-07-13 | [#145](https://github.com/pilotspace/hydroa/issues/145) | service-tiers R5 rounding boundary: priority_cap+standard_cap can exceed cluster_cap by 1 when priority_pct+standard_pct near 1.0 (R5 only rejects sum>1.0); non-default config only, per-pool caps still enforced. Consider clamp or tighten R5. |
| 29 | 2026-07-13 | [#146](https://github.com/pilotspace/hydroa/issues/146) | service-tiers check_and_hold: self._holds[request_id] overwritten if called twice for same request_id (not currently reachable — single call site). Guard against double-hold if a second call site is ever added. |
| 30 | 2026-07-13 | [#147](https://github.com/pilotspace/hydroa/issues/147) | bedrock-region-guard case-sensitivity coupling: _assert_region_consistent is case-sensitive by design; uppercase-prefix bypass (EU.) is unreachable ONLY because model_checker.py:64 does a case-sensitive catalog PK match first. If that lookup is ever relaxed (citext/case-insensitive), the region guard becomes a live bypass. Add a cross-file invariant test or a comment pinning the coupling. (from security verify A) |
| 31 | 2026-07-13 | [#148](https://github.com/pilotspace/hydroa/issues/148) | residency BYOK region cross-check (from security verify B): BedrockCredential.region (tenant-supplied BYOK) is NOT validated against tenants.residency_region at credential create/update time. For an UNPREFIXED bedrock model that passes residency-policy (catalog region=eu), a pinned tenant who registered their own Bedrock credential with region=us-east-1 would still dial US. Tenant self-misconfiguration only (not cross-tenant/attacker-reachable, outside bedrock-region-guard's frozen M3 scope). Fix = validate BedrockCredential.region vs tenants.residency_region at BYOK registration for pinned tenants. Candidate residency-policy follow-on. |
| 32 | 2026-07-13 | [#149](https://github.com/pilotspace/hydroa/issues/149) | bedrock-region-guard defense-in-depth (verify B, low-pri): normalize model_id prefix casing/whitespace before _PROFILE_PREFIX_TO_GEO lookup — currently protected solely by the catalog exact-match dispatch gate; cheap insurance vs a future refactor decoupling model_id sourcing from routing. |
| 33 | 2026-07-13 | [#150](https://github.com/pilotspace/hydroa/issues/150) | residency-tiers-ui (observe, 💭): residency-picker radios use min-h-11 (44px hit target) but lack a dedicated hit-target assertion test (TierSelector has one). Add for parity. |
| 34 | 2026-07-13 | [#151](https://github.com/pilotspace/hydroa/issues/151) | residency-tiers-ui (observe, 💭): TierSelector.tsx line 38 (standard-after-priority toggle) untested — symmetric to tested line 50, low-risk. Add test for coverage parity. |
| 35 | 2026-07-13 | [#152](https://github.com/pilotspace/hydroa/issues/152) | residency-tiers-ui (observe, 💭): no live screen-reader (VoiceOver/NVDA) pass on new residency/tier surfaces — axe + native semantics + live keyboard trace substitute but aren't equivalent. Consider a manual SR pass pre-GA. |
| 37 | 2026-07-13 | [#153](https://github.com/pilotspace/hydroa/issues/153) | semantic_cache flake instance (2026-07-13 M2 pre-merge): test_semantic_hit_ledger_cached_true_cost_zero + test_semantic_cache_normalized_variant_hits failed in the full suite (shared Redis :6380 db 9 cross-test contamination — a prior test's cache entry made 'first' request a hit, cost 0). Pass 15/15 isolated. Known flake class. The xdist CI work (task #11) should give each worker its own Redis db/namespace to KILL this class permanently. |
| 40 | 2026-07-14 | [#154](https://github.com/pilotspace/hydroa/issues/154) | agent-identity: decide whether agent-principal OAuth tokens should authenticate to /v1/realtime (today they silently cannot — realtime_ws/realtime_relay_ws build bare SqlAlchemyKeyAuthenticator, not CompositeKeyAuthenticator). Verify-surfaced spec-delta, 2026-07-14. |
| 41 | 2026-07-14 | [#155](https://github.com/pilotspace/hydroa/issues/155) | agent-budget: add a chat-pipeline live test mirroring test_disconnect_correction_reaches_agent_principal_counter_via_recovery_sweep step-4 against CompletionUseCase._check_agent_principal_budget via a real /v1/chat/completions call (currently only NonChatGovernance.authorize path is live-tested; chat copy verified by code-diff parity). Low priority, 2026-07-14. |
| 42 | 2026-07-14 | [#156](https://github.com/pilotspace/hydroa/issues/156) | compliance-report-center (verify a854c67d, 🟡→ticket): RetentionSweeper.should_start_retention_sweep gates ONLY on the 3 original knobs (usage/alert/audit days) — if an operator zeroes all three, the whole sweeper loop never starts, INCLUDING the new compliance_report_runs window + ZDR purge passes (pre-existing from tenant-retention-zdr, surfaced by R1). Fix: start-gate should also consider new-payload-window/ZDR-purge applicability. |
| 43 | 2026-07-14 | [#157](https://github.com/pilotspace/hydroa/issues/157) | compliance-report-center (verify open-Q + TASK.md §1 low-conf flag): generated compliance EVIDENCE (compliance_report_runs) is swept on the SAME retention_window_days as operational logs — an Art.12 bundle may need LONGER retention than a tenant's short log window. Consider a separate retention_report_runs_days knob (carve-out) if EU-tenant demand shows (monitor: rising skipped_zdr / never-success share). |
| 44 | 2026-07-14 | [#158](https://github.com/pilotspace/hydroa/issues/158) | tool-call-metering (accepted v1 residual follow-on): exactly-once is fail-open under Redis outage (SETNX dedupe in MeteringToolCallObserver, not recorder ON CONFLICT — frozen recorder sig has no call_id passthrough). Add a DB-level UNIQUE backstop on a call_id-derived tag so a duplicate record() during a Redis outage can't double-bill. NOT a 0.9.0 blocker (disclosed). |
| 45 | 2026-07-14 | [#159](https://github.com/pilotspace/hydroa/issues/159) | mcp-connector-passthrough budget CR (deferred at freeze): NonChatGovernance.check_budgets() enforces the per-key/team/agent-principal/tenant $ ladder but NOT RPM/TPM rate limits or credit-hold — deferred because there is no MCP settle hook yet. Wire RPM/TPM + credit-hold for /v1/mcp/call once a settle/refund path exists. |
| 46 | 2026-07-14 | [#160](https://github.com/pilotspace/hydroa/issues/160) | agents-console (follow-on CR): ManageTokensDialog / the agent-tokens endpoint enumerate ONLY tokens already ATTACHED to a principal; a token created-but-unattached is invisible. Decide whether to surface unattached tokens (broaden the enumeration) or document the attach-first flow explicitly. |
| 47 | 2026-07-14 | [#161](https://github.com/pilotspace/hydroa/issues/161) | ADD docs-check (R1 milestone-close gap): runbooks 02-admin-guide / 03-api-client-guide / 06-dashboard-ui do NOT yet cover the new agent-gateway surfaces (/v1/messages ingress, MCP connector allow-lists, Agents console, Compliance report center). R1 was additive so nothing is stale-WRONG, only incomplete. Add sections in a docs-only sweep. |
| 49 | 2026-07-16 | [#162](https://github.com/pilotspace/hydroa/issues/162) | CatalogModel.cached_input_usd_per_token + audio_* fields are typed float\|None while pricing/unit fields are Decimal\|None — money-is-Decimal is non-uniform across CatalogModel's own fields (pre-existing, flagged in catalog-db-seed §0, NOT introduced by it). Follow-up: migrate those to Decimal for uniformity. Out of catalog-db-seed's frozen scope. |
| 51 | 2026-07-17 | [#163](https://github.com/pilotspace/hydroa/issues/163) | self-serve-checkout F1: amend frozen M3 to thread the checkout session PK (not the tenant-chosen idempotency_key) into topup_service — the credit_ledger GLOBAL unique index makes the tenant key a cross-tenant existence oracle + squatting-DoS surface (fail-closed, non-exploitable with UUID keys today). Contract change-request. |
| 52 | 2026-07-17 | [#164](https://github.com/pilotspace/hydroa/issues/164) | self-serve-checkout: enforce checkout_sessions.expires_at at confirm (or a sweeper) so pending sessions become 'expired' past TTL; add the failed-state transition + the confirm-expired-session scenario/test. Today expires_at is write-only and expired/failed are unreachable. |
| 53 | 2026-07-17 | [#165](https://github.com/pilotspace/hydroa/issues/165) | self-serve-checkout: stripe_provider.py behavioral tests via a stub httpx transport (retry+backoff, timeout, happy-path capture) — coverage 48%, only the breaker-open path is asserted. Also add a Stripe Idempotency-Key header on the create POST before stripe goes live (retried create can spawn duplicate hosted sessions, not charges). |
| 54 | 2026-07-17 | [#166](https://github.com/pilotspace/hydroa/issues/166) | commercial-self-serve: seat_change checkout variant (deferred at self-serve-checkout freeze) — additive CheckoutIntent variant + proration/seat invoice-line interplay; and tenant-name enrichment for the invite email (transactional-email observe delta). |
| 57 | 2026-07-24 | [#167](https://github.com/pilotspace/hydroa/issues/167) | R5 candidate: multi-provider moderation detectors — second adapter behind the ml-moderation verdict seam (Llama Guard 3 via OpenRouter/Bedrock, or Gemini safety classifier); enterprise differentiator vs OpenAI-only; approved-in-principle by Tin 2026-07-24 |
| 66 | 2026-07-25 | [#168](https://github.com/pilotspace/hydroa/issues/168) | R6 pgvector-deploy-runbook DEPLOY BLOCKER: infra/docker-compose.prod.yml + charts/ai-proxy/values.yaml swap postgres:16-alpine -> pgvector/pgvector:pg16 = musl->glibc. Booting the Debian image on an Alpine-initdb'd volume is a collation-provider change: text btree indexes potentially mis-ordered until REINDEX. Needs dump/restore or REINDEX DATABASE + ALTER DATABASE ... REFRESH COLLATION VERSION in the runbook, not a silent tag bump. |
| 67 | 2026-07-25 | [#169](https://github.com/pilotspace/hydroa/issues/169) | R6 pgvector-deploy-runbook: migration 55dc3f920a38 runs CREATE EXTENSION IF NOT EXISTS vector — on RDS/Aurora/Cloud SQL this needs rds_superuser + an allow-listed extension or the migration hard-fails at deploy. Document the prereq AND add a boot-time preflight that fails loudly when the vector extension is absent. |
| 70 | 2026-07-25 | [#170](https://github.com/pilotspace/hydroa/issues/170) | R7: 410 pre-existing per-line '# noqa: CODE' directives across apps/gateway carry NO written reason (143 do). lint-type-debt-sweep fixed only the 3 it added; a guard requiring justification on all of them would be a real scope of its own. Consider a ratchet: new noqas must be justified, existing ones grandfathered. |
| 71 | 2026-07-28 | [#171](https://github.com/pilotspace/hydroa/issues/171) | ruff EXCLUDE list is a lint blind spot: the 63 test files excluded by lint-type-debt-sweep CR v2 get NO ruff check at all, so a missing import in one reads as 'All checks passed'. Hit for real on 2026-07-26 editing tests/semantic_cache (missing 'import time', caught only by reading the imports). Options: re-include with per-file format-only ignores, or add a syntax/name-resolution check that covers excluded files. |
| 72 | 2026-07-28 | [#172](https://github.com/pilotspace/hydroa/issues/172) | pytest-timeout (suite-stability M8) bounds a TEST, not the SESSION: a stall in session teardown, after the last test, is still silent. If a teardown hang ever shows up, add an outer wall-clock kill in the CI job rather than widening the plugin's remit. |
| 73 | 2026-07-28 | [#173](https://github.com/pilotspace/hydroa/issues/173) | tests/realtime bootstraps its own schema and bypasses the shared conftest app/_schema fixtures entirely (own settings, own drop_all/create_all, own TestClient). It now does that BEFORE the lifespan starts, but it is still a second, divergent isolation mechanism. Fold it onto the shared session-scoped _schema + DELETE reset so there is one reset path, not two. |
| 74 | 2026-07-28 | [#174](https://github.com/pilotspace/hydroa/issues/174) | Fixed-sleep-then-assert flake class, 7 files still carrying it: guardrails/test_guardrails_core, health_alerting, mcp_connector/test_budget_governance, mcp_connector/test_call_flow, obs_callbacks, payload_capture/{test_payload_capture_store,test_verify_repro_scrub_gap}, request_log_metering_fields. They sleep a fixed 0.1s then assert on a fire-and-forget write — passes idle, fails under -n 6 on a loaded host. Both semantic_cache instances were converted to bounded polls in suite-stability (they actually failed); the rest are latent. Same class as [[fire-and-forget-audit-test-flake]]. Convert to poll-until-present per file. |
| 76 | 2026-07-28 | [#175](https://github.com/pilotspace/hydroa/issues/175) | MIXED CLOCK SOURCES on updated_at — PARTIALLY FIXED in suite-stability CR v3 (2026-07-28): 9 write sites in batches/conversations/finetune/video now use func.now(), guarded by repo_hygiene::test_timestamp_columns_have_one_clock_owner. REMAINING: (a) compliance/infrastructure/repository.py:54-55 takes 'now' as a PARAMETER, so the guard cannot see the clock at the call site — audit its callers; (b) created_at is only covered where it is written explicitly, and the guard's scope is */infrastructure/ so a write from an application-layer service would be missed. Two candidates were checked and REJECTED as false positives: credits/api/router.py:211 (response field, not a column) and credits/infrastructure/ledger_store.py (already SQL now()). |
| 77 | 2026-07-29 | [#176](https://github.com/pilotspace/hydroa/issues/176) | Tests ordering by a func.now() column cannot break ties: usage_records.created_at is server_default=func.now() (transaction-START time), so rows written in one flusher transaction share a timestamp and ORDER BY created_at is nondeterministic. Fixed in tests/semantic_cache SC1+SC9 by identifying rows by their cached marker. No repo_hygiene guard exists for this class — a naive ban on 'ORDER BY created_at' in tests would false-positive on the many suites that order across separate transactions. Needs a smarter guard (e.g. flag ORDER BY created_at where the test also asserts on rows[0]/rows[1] positionally). |
| 79 | 2026-07-29 | [#177](https://github.com/pilotspace/hydroa/issues/177) | Fixed-sleep-then-assert remains the suite's largest flake class: 250 asyncio.sleep sites in tests/, of which ~83 are positive waits (pollable via tests/_polling.py), 29 are NEGATIVE (the sleep proves something never happens — polling would make the assert vacuous, do NOT convert), 89 need per-site judgement. suite-stability converted 21. The rest still fail randomly under -n 6 on a loaded host — each full run surfaced a DIFFERENT subset. Sweep the remaining positive waits; the classifier is at scratchpad/classify.py and the conservative converter at scratchpad/convert.py (note: convert.py's import insertion breaks multi-line parenthesized imports — fix before reuse). |
| 81 | 2026-07-29 | [#178](https://github.com/pilotspace/hydroa/issues/178) | suite-stability M14 residual: tests/catalog_refresh_scheduler::test_default_off_not_wired stalled 300s once under -n 6 (2026-07-29 ev1) and did NOT reproduce in the next full run. Evidence brackets it to late lifespan STARTUP or the first shutdown cancel-join; the join is now bounded (M14), the startup branch is not. Suspected mechanism: a startup DB read queued behind per-test DDL (pg sampler caught real relation-lock waits of 0.5s and 3.7s on gw3). If it recurs, the stallwatch plugin recipe is in the task notes. Owner: Tin Dang |
| 82 | 2026-07-29 | [#179](https://github.com/pilotspace/hydroa/issues/179) | todo #79 concrete sites: tests/response_caching/test_response_caching.py:393 and :1074/:1089 still use bare asyncio.sleep(0.1/0.15) with the comment 'allow fire-and-forget recording'. Failed as 'expected 2 usage rows, found 1' in a slow full run (12m59s vs the usual 6m19s) on 2026-07-29; passes in isolation. Convert to tests._polling.poll_until, the same fix suite-stability M10 applied to the four usage/team_attribution sites. Owner: Tin Dang |
| 87 | 2026-08-07 | [#180](https://github.com/pilotspace/hydroa/issues/180) | Dev-stack port 5433 is contended with the real-estate-management repo's infra project (which also holds 5432 and 5434). Only one of the two stacks can run at a time, and Docker will NOT re-publish a port after a failed bind — the loser comes back healthy with an EMPTY Ports column (docker port returns nothing, NetworkSettings.Ports is {}), which reads as working and is unreachable. Only fix is docker compose up -d --force-recreate from that project. Make hydroa's host port configurable (POSTGRES_PORT:-5433 in infra/docker-compose.dev.yml + GATEWAY_TEST_DATABASE_URL) so the two stacks can coexist. NOTE retargeting via env alone does NOT work: tests/ops/test_probes.py builds a bare Settings() hardcoded to localhost:5433, and adding GATEWAY_DATABASE_URL breaks 42 migration tests with DuplicateTableError. |
| 89 | 2026-08-07 | [#181](https://github.com/pilotspace/hydroa/issues/181) | release-integrity exit #6 (three CONSECUTIVE green make ci) BROKEN at run 3, 2026-08-07. Runs 1+2 clean (4531 passed); run 3 = 4530 passed + 1 ERROR at setup of tests/per_key_guardrail_policies::test_put_rejects_invalid_mode. SUPERSEDES todo #88, whose diagnosis (pytest-timeout's 300s signal) was WRONG — the log contains ZERO pytest-timeout reports. VERIFIED mechanism: the top frame is asyncpg connection.py:2443 'async with compat.timeout(timeout)' = asyncpg's own DEFAULT 60s CONNECT timeout, not 300s. It fired at connect_utils.py:977, inside _create_ssl_connection, on 'await pr.on_data' — i.e. TCP was ESTABLISHED and the SSLRequest byte was written, and Postgres never answered the SSL probe within 60s (asyncpg sslmode defaults to 'prefer', so SSL is always attempted first). It surfaced as a raw CancelledError instead of TimeoutError because SQLAlchemy's greenlet bridge (_concurrency_py3k.py:201) re-raised it across the boundary. Caller is tests/conftest.py:331 — the app fixture's per-test DELETE reset opening a pooled connection. Server side shows NO trace: no 'too many clients', no fork failure, max_connections=100 with 6 in use; the only FATALs in the postgres log are the vector_extension_preflight fixture's own deliberate absent-DB probe and its DROP DATABASE WITH FORCE teardown. So: a backend that accepted the socket but never got scheduled to reply. Contention is the live hypothesis (four postgres containers on the host, todo #87). Same family as todo #81. NOT an infra dropout — 0 'Connect call failed', 0 tripwire fires. |
| 90 | 2026-08-07 | [#182](https://github.com/pilotspace/hydroa/issues/182) | atomicity: date-bomb-sweep — §3 scope reads as 2 Parts (catch-all, drain); consider new-milestone + one task per Part. |
| 91 | 2026-08-07 | [#183](https://github.com/pilotspace/hydroa/issues/183) | breaker 4xx residue (promised by breaker-4xx-classification PLAN.md §3 but never filed): objectstore/s3.py:115 calls self._breaker.on_upstream_error() BEFORE reading the status, so an S3 403 from bad/expired credentials counts toward the breaker — 5 of them open the object-store breaker for 30s on a caller error. _mutate (s3.py:136) counts every ClientError the same way. email/infrastructure/smtp_email_sender.py counts every failure DELIBERATELY (comment says so) — confirm that is still wanted. Same bug class as todo #60, different subsystems, own decisions. |
| 92 | 2026-08-07 | [#184](https://github.com/pilotspace/hydroa/issues/184) | Collapse the ~10 bare '>= 500' breaker literals onto status_counts_as_upstream_failure (added by breaker-4xx-classification). azure_embeddings.py:206/261/322, gemini_upstream.py:964/1126, openrouter_upstream.py:335, vertex_upstream.py:314, azure_upstream.py:238, anthropic_upstream.py:991 all gate the breaker on 'status >= 500' and call record_success() otherwise — so they treat 408 and 429 as SUCCESS while the four sites fixed in #96 count them as upstream failure. Both directions are defensible; having both in one codebase is not. The predicate's own docstring names collapsing RetryPolicy.classify_status onto it as the right end state. |
| 94 | 2026-08-07 | [#185](https://github.com/pilotspace/hydroa/issues/185) | kind-e2e is now workflow_dispatch-only (PR #98), honoring ci-restoration CR v2 — but NOTHING enforces the other half of that decision, 'runs before a release cut'. It is a manual step with no reminder and no entry in any release checklist, and it has 0 green runs in 15 attempts, so its first on-demand run should be expected to need debugging rather than to yield a verdict. Either add it to the milestone Release steps checklist or give it a tag/schedule trigger. Filed during the #98 self-review. |
| 96 | 2026-08-08 | [#186](https://github.com/pilotspace/hydroa/issues/186) | MEASURED 2026-08-08, run 31243949907 — the gateway suite's first EVER completed CI run: 8 failed, 4553 passed, 7 skipped, 1 xfailed in 4935s (1h22m15s) at -n 4 with coverage. Key numbers: (a) set timeout-minutes to ~120 (82m * 1.5) and raise MIN_GATEWAY_TIMEOUT_MINUTES to match — supersedes the 180 instrument in todo #95; (b) -n 4 bought almost NOTHING vs serial (serial was cancelled at 74m17s still running; -n 4 completed at 82m25s), because ubuntu-latest's 4 vCPUs are shared with the Postgres+Redis service containers and every worker carries 39% coverage overhead while contending on ONE database. Adding workers to this runner is a dead end; the only real fixes are matrix-sharding with a DB per shard, or a bigger runner. All 8 failures are PRE-EXISTING (PR #98 touches zero production source): 5 map to already-filed issues (#74 names guardrails/test_guardrails_core and request_log_metering_fields; the Makefile test-parallel comment names the realtime DDL deadlock; v58 flags the batch claim/drain lifecycle). The other 3 (agent_identity_governance kill-race, s3_egress_policy IPv6-mapped literal, tool_call_metering/test_di_wiring) all pass locally in isolation and need classification. |
| 100 | 2026-08-10 | [#187](https://github.com/pilotspace/hydroa/issues/187) | R7 PERF (supersedes the closed #39, whose premise was already fixed): the gateway suite's dominant per-test cost is FIXTURE SETUP, not the DB reset. MEASURED 2026-08-10 on a 12-core/24GB host: (a) create_app() costs 194ms every call (5 iterations, first 220ms, min 154ms) and tests/conftest.py::app calls it PER TEST -> 4559 x 194ms = ~15min of pure app construction per full run, before coverage; (b) coverage is a 1.92x multiplier, NOT the 39% recorded in todo #96 -- same 182 tests took 82.04s with --cov vs 42.70s with --no-cov at -n 4 --dist loadscope; (c) --durations on tests/conversations shows per-test setup at 0.43-1.20s while ZERO 'call' entries clear a 50ms threshold, so test bodies are not the cost; (d) the per-test DELETE sweep is 11ms (suite-stability's own measurement, conftest.py:294) and is NOT worth optimizing; (e) the unaccounted ~225ms/test is per-test create_async_engine + engine.dispose(), i.e. a fresh Postgres connection pool per test. FIX DIRECTION: widen the app fixture's scope (module or session) with an explicit app.state reset, and/or reuse one engine per worker. HAZARD, why this was NOT done in flake-tail-burndown: tests mutate app.state directly to inject fakes (app.state.completion_upstream = fake), and app.state-level bleed across tests is exactly todo #99's UNEXPLAINED routing_admin cross-suite leak -- widening app sharing before #99 is grounded would make that class worse in a way that only surfaces weeks later. Sequence #99 first. Not needed for release-integrity exit #6: running the proving runs with --no-cov already halves them, and exit #6 says 'full gateway suite', not 'make ci'. |
| 101 | 2026-08-10 | [#188](https://github.com/pilotspace/hydroa/issues/188) | Test-hygiene, DETERMINISTIC class (residual risk accepted by flake-tail-burndown CR v2, 2026-08-10): 35 tests/ sites use 'await asyncio.sleep(0)' as a one-shot event-loop yield before asserting on a fire-and-forget result. That is CORRECT and load-independent when the awaited coroutine has no internal awaits -- verified for the billing path: use_cases.py:543 fires asyncio.ensure_future(usage_recorder.record(**kwargs)) and the SpyRecorder double is 'async def record(...): self.calls.append(...)', zero awaits, so it completes on its FIRST scheduling step. All 10 sites in tests/image_edits_variations are this shape and none has ever appeared in a CI failure. BUT the property is a fact about the CALLEE, not the test: if any of the 35 sites' fire-and-forget target ever gains a real await (a DB write, a Redis call, an HTTP hop), one yield stops being sufficient and the assertion breaks DETERMINISTICALLY -- on an idle laptop too, so it would be a hard red rather than a rotating flake, which is exactly why M7's guard deliberately does NOT cover it (M7 targets guessed DURATIONS). The 35 are enumerated by inverting the M7 guard's own literal>0 filter. Right fix if it ever bites: convert that specific site to tests._polling.poll_until like a positive wait; do NOT widen M7 to flag sleep(0) globally, which would mark 35 correct sites as debt and dilute the guard's signal. |
| 103 | 2026-08-10 | [#189](https://github.com/pilotspace/hydroa/issues/189) | tests/credits_ledger/conftest.py exports its OWN poll_until with a DIFFERENT signature (single truthy predicate) that shadows tests._polling.poll_until on import. The new flake-sweep call site is spelled _polling.poll_until(...) with a note rather than rebinding a name 3 other tests in that suite depend on. Collapse the duplicate onto the shared primitive. |
| 109 | 2026-08-11 | [#190](https://github.com/pilotspace/hydroa/issues/190) | MASKED GATE 3 — kind-e2e has NEVER passed: 30 runs, 30 failures, 0 success, ever (gh run list --workflow=kind-e2e.yml). Its own header admits '0 green in 15 attempts'; the true figure is now 0/30. It is workflow_dispatch-only, so it reports nothing on PRs. This matters because kind-e2e is the DECLARED automated proof of the e2e exit criterion — a criterion whose proof surface has never once produced a pass. Either make it green or stop describing it as the proof; a permanently-red opt-in job is indistinguishable from no job. R7-sized (45-min k8s pipeline, needs real debugging), not a quick fix. |
| 114 | 2026-08-11 | [#191](https://github.com/pilotspace/hydroa/issues/191) | CLOSED NEGATIVE, do not re-try: COVERAGE_CORE=sysmon cannot remove the ~1.92x coverage multiplier here. Tested 2026-08-11 on coverage 7.14.1 / Python 3.12.13. coverage.py REFUSES the sys.monitoring core whenever concurrency=greenlet is set, which apps/gateway/pyproject.toml [tool.coverage.run] sets because SQLAlchemy's async bridge runs ORM code inside greenlet switches: 'CoverageWarning: Cant use core=sysmon: it doesnt support concurrency=greenlet, using default core (no-sysmon)'. Note the failure mode -- it FALLS BACK with a warning rather than erroring, so setting the env var in CI and judging by wall-clock alone would show no improvement with no visible reason. Dropping concurrency=greenlet would let sysmon engage but would under-report every async DB code path, i.e. buying speed by weakening the measurement the 80% gate depends on -- not acceptable. The remaining real levers stay: shard/concurrency-slot tuning, and the create_app + per-test-engine fixture work in todo #100 (still sequenced behind todo #99). |
| 116 | 2026-08-11 | [#192](https://github.com/pilotspace/hydroa/issues/192) | flake class 10 residue OUTSIDE the #111 pin: tests/edge/test_e2e_edge.py:369 asserts 'elapsed < 3.0' over 60 real HTTP round-trips through Docker+Envoy, as a proxy for 'all 60 landed inside one rate-limit window'. #111's in-process clock pin CANNOT reach it — the limiter there is Envoy's http_local_rate_limit filter in a SEPARATE process, not a Python class, so there is no __init__ to wrap and no injectable now. The causal replacement is to read Envoy's own counter instead of the wall clock: GET the admin /stats endpoint and assert http_local_rate_limit.rate_limited equals 60 - max_tokens, which is a fact about the filter rather than a bet on network speed. Deferred, not forgotten: the edge suite is dispatch-only (make e2e-edge), has never run under -n 12, and reaching Envoy admin from the test needs a port publish in infra/docker-compose.edge.yml — a real change in a suite that is not part of the default gate. Until then the site stays explicitly marked NOT MEASURED rather than given a fabricated margin. |
| 117 | 2026-08-12 | [#193](https://github.com/pilotspace/hydroa/issues/193) | RELEASE ARTIFACT ARCH GAP — docs/runbooks/cloud-deploy.md builds images with a bare `docker build`, which produces an image for the OPERATOR'S HOST ARCH. MEASURED 2026-08-12: on an Apple-silicon host both images built `linux/arm64` (gateway + dashboard, from tag v0.14.1). Pushed as 0.14.1-prod to a typical amd64 EKS/GKE/AKS node pool that image fails at DEPLOY time with 'exec format error', not at build time — so the defect surfaces after the artifact is published and the tag already means the wrong thing. NOTHING catches it at any layer: the runbook has zero mention of platform/arch/buildx, charts/ai-proxy declares no nodeSelector or arch affinity, and no CI job builds or publishes images at all (todo #110). Same class as the defects R6 closed: a release artifact that lies about what it is. FIX APPLIED to the runbook (buildx multi-arch, linux/amd64+linux/arm64). REMAINING, not done here: (a) nothing ENFORCES the fix — a guard should assert the published manifest lists amd64, but there is no publish step in CI to hang it off, so this stays a human discipline until #110 lands an image-publish workflow; (b) decide whether the chart should declare an arch nodeSelector so a wrong-arch image is rejected at schedule time rather than at container start; (c) the amd64 half is built under QEMU emulation on arm64 hosts — slow, and 'it built' is not 'it runs', since no amd64 host here can execute it. The first real amd64 execution will be the deploy itself. |
| 119 | 2026-08-12 | [#194](https://github.com/pilotspace/hydroa/issues/194) | list |
| 120 | 2026-08-12 | [#195](https://github.com/pilotspace/hydroa/issues/195) | R7 contract-surface deltas from the PR#89 residue batch (branch fix/vector-store-residue, PRs after #119): THREE reject/error codes were ADDED to closed, frozen §3 contracts rather than editing the frozen PLAN.md files. file-search-tool: 'file_search_multiple_vector_stores_unsupported' (422, todo #61 — >1 vector_store_ids was silently served from ids[0]) and 'file_search_query_missing' (422, todo #65 — a body with no user text embedded "" and billed a per_query). vector-store-files: last_error.code 'storage_unavailable' (todo #65 — an ObjectStore outage reported as file_empty). Each is documented in-code with the reasoning and in the PR body. If either task is ever re-opened or re-frozen, its §3 must absorb these; a client-facing error-code list must list them too. |
| 121 | 2026-08-12 | [#196](https://github.com/pilotspace/hydroa/issues/196) | R7 SCALE follow-up from todo #62: vector_store_chunks retrieval recall now depends on THREE things nothing enforces at deploy time — (1) ix_vector_store_chunks_store existing (guarded in CI by tests/file_search_tool/test_retrieval_repo.py, NOT in prod), (2) current table statistics (a bulk ingest without ANALYZE can flip a selective store onto the HNSW post-filter plan), (3) pgvector >= 0.8 for hnsw.iterative_scan (an older server logs file_search_iterative_scan_unavailable once and silently degrades). Wire the alert: file_search_topk_shortfall (WARNING, gateway.vector_stores.infrastructure.file_repository) has no alert rule anywhere yet — the bound is published in docs/runbooks/pgvector-deploy.md §7 but nothing pages on it. Also unbuilt: partitioning vector_store_chunks by vector_store_id, the documented escape hatch for a store large enough to exceed hnsw.max_scan_tuples (needs a migration). |
| 122 | 2026-08-12 | [#197](https://github.com/pilotspace/hydroa/issues/197) | FLAKE (CI-only, 2026-08-12, PR #120 run 31577609140): tests/pii_v2/test_pii_v2.py::test_builtin_iban_masks_pre_call ERRORed with 'Failed: Timeout (>300.0s) from pytest-timeout' AT FIXTURE SETUP on gateway shard 2 — the loop was parked in epoll.poll(timeout=-1), i.e. blocked forever on a socket with NO timers pending, so something in client/app/active_model/redis_client setup awaited a connection that never answered. Shard reported 1167 passed / 1 error; a --failed re-run went fully green and the suite is 20/20 locally, so the merge was NOT gated on a real defect. Cost: ~10min of a shard plus a blocked merge. NOT the absolute-wall-clock class (todo #105) and not a sleep site — it is an unbounded await in test SETUP, so the fix shape is a deadline on whichever fixture connects (the lifespan-shutdown-bounded lesson applied to setup), not an AST guard. Next occurrence: capture which fixture with --timeout-method=thread and -o log_cli=true. |
| 123 | 2026-08-12 | [#198](https://github.com/pilotspace/hydroa/issues/198) | ⚠ DO NOT run `npx @pilotspace/add@latest update` on this bundle without a decision first (learned 2026-08-12). ADD 3.x is a DIFFERENT bundle format (ABF-1) and does NOT convert 2.x: after updating, `cli.py status --check` says 'this is an ADD 2.x bundle — 3.0 reads a different format and does not convert it … archive them as the record of how this was built, then start a 3.0 bundle / next: add init'. So the update REPLACES the working engine (add.py becomes an ABF-1 library, add_engine/* deleted) and leaves the project unrunnable until you either roll back or init an EMPTY 3.0 bundle, discarding continuity of 94 archived milestones / 419 tasks / PROJECT.md invariants / 9 personas as a read-only archive. The engine's own `status` nag says 'ACTION REQUIRED before the user's task' — following it BREAKS the workflow it claims to protect. Rollback recipe: `git checkout -- .add .claude`, delete the untracked additions (.add/personas-index/, .add/tooling/cli.py, .claude/skills/add/{domains,gate,personas,seed,streams}.md, phases/explore.md) — AND restore .add/tooling from a physical backup, because .add/.gitignore ignores tooling/ so add_engine/search.py is NOT tracked and git alone leaves the engine broken (ModuleNotFoundError: add_engine.search). Take a `cp -R .add` snapshot before ever running it. |
