# WAVE-1 shared context — api-surface-parity direction drafting (2026-07-24)

Orchestrator-owned. Read FIRST. Do NOT edit this file, MILESTONE.md, state.json, or any other task's files.

## Mission
Milestone `api-surface-parity` (R4, feeds 0.12.0): any OpenAI-SDK app points its base URL at Hydroa
and Responses/Files/Moderations/Image-edits/Usage calls work — tenant-scoped, exactly billed, honoring
every existing governance gate. Full scope + shared decisions + Ground:
`.add/milestones/api-surface-parity/MILESTONE.md` (READ IT — your §1 PROJECTS from its `## Ground`).

## Your beat: DIRECTION only
- Phase guide: `/Users/tindang/.claude/skills/add/phases/direction.md` (read it; follow §1 → §3 → §4).
- Draft the WHOLE bundle in ONE write into YOUR task's `.add/tasks/<your-slug>/PLAN.md` (template
  already scaffolded there — fill it in place, keep the template's section markers/tag vocabulary).
- Write the §4 red suite as REAL test files under `apps/gateway/tests/<your_slug_underscored>/`
  (project convention: one dir per task) and RUN it — it must fail for the RIGHT reason
  (missing implementation), evidence pasted into PLAN.md §4.
- Leave §3 `Status: DRAFT`. NEVER run `add.py freeze | advance | gate | use` — the freeze is the
  human's gate; the orchestrator presents your card.
- End with the freeze card content (banner → ARC → SHAPE → SUMMARY → FLAGS lowest-confidence-first →
  DECIDED → EVIDENCE → APPROVE → NEXT) as your final report.

## Hard project rules (non-negotiable)
- GROUND by live reads (serena MCP tools preferred), never memory; cite SYMBOLS not line numbers;
  tag claims [OBSERVED]/[DERIVED]/[PRIOR]/[ASSUMED].
- Byte-identical default path: a request not using your new surface engages ZERO new plumbing.
- Every proxied inference request → exactly ONE usage record, billed on the SERVED model via the
  existing pricing_unit dispatcher; surfaces with no provider cost write NO usage record.
- No outbound IO without timeout + bounded retry (idempotent only) + circuit breaker.
- Tenant scoping on every row; cross-tenant reads return 404 (never a leak, never a 403/404 alternation).
- New payload-at-rest stores MUST compose with ZDR (metadata-only), retention sweeper, residency,
  and the payload-capture inventory; new tables go in BOTH manifests (tests/migrations EXPECTED_TABLES
  + guardrails NOT-IN list).
- New /v1 routes ride the existing Envoy ext_authz gate (check `infra/` envoy config for route coverage).
- Pyright strict (`make ci` runs it); TypedDict/typed extras seam — no inspect.signature dispatch.
- Tests: red/green TDD; shared test postgres :5433 needs a unique GATEWAY_TEST_DATABASE_URL;
  don't run the full suite — only your task dir's suite.

## Key anchors (verify live before citing)
- Chat seam: `apps/gateway/src/gateway/proxy/api/router.py` + ChatTranslator (v9) + extract_usage_from_sse.
- Second-native-wire precedent: `proxy/api/messages_router.py` (/v1/messages Anthropic ingress).
- Images seam: `proxy/api/images_router.py` + images_deps.py; NonChatGovernance choke point.
- ML moderation: `proxy/infrastructure/ml_moderation_evaluator.py` (isolated breaker, `unchecked` verdict).
- ObjectStore port: `objectstore/` + `artifacts/` (api/router.py, infrastructure/repository.py).
- State-store precedent: `conversations/` (tenant-scoped conversation + message rows).
- Batches consumer: `batches/` (api/router.py, infrastructure/orm.py) — files task adds `input_file_id`.
- Usage ledger: `usage/` (usage_records, recorder).

## Sibling wave-1 tasks (do not build their scope; name the seam if you touch it)
responses-api-core · files-uploads-api · moderations-endpoint · image-edits-variations ·
tenant-usage-costs-api. Wave-2 (NOT yours): responses-state-store (stored responses/chaining —
responses-api-core must design its wire so state can attach WITHOUT re-freezing the core contract;
note the extension point, don't build it).

## Report discipline
Self-score (6 dims, refine weakest <0.9 before reporting). Your final message = the freeze card,
nothing else load-bearing outside it.
