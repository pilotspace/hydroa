# WAVE-1 shared context — managed-rag-finetune direction drafting (2026-07-24)

Orchestrator-owned. Read FIRST. Do NOT edit this file, MILESTONE.md, state.json, or any other task's files.

## Mission
Milestone `managed-rag-finetune` (R5, feeds 0.13.0): a tenant uploads files → builds a managed vector
store → retrieves via `file_search` inside a Responses/chat call → brokers a fine-tune job to its BYOK
provider — all OpenAI-SDK-compatible, tenant-scoped, exactly billed. Full scope + shared decisions +
Ground: `.add/milestones/managed-rag-finetune/MILESTONE.md` (READ IT — your §1 PROJECTS from its `## Ground`).

## Your beat: DIRECTION only (this is a FREEZE-FIRST task)
- Phase guide: `/Users/tindang/.claude/skills/add/phases/direction.md` (read it; follow §1 → §3 → §4).
- Draft the WHOLE bundle in ONE write into YOUR task's `.add/tasks/<your-slug>/PLAN.md` (template
  already scaffolded there — fill it in place, keep the template's section markers/tag vocabulary).
- Write the §4 red suite as REAL test files under `apps/gateway/tests/<your_slug_underscored>/`
  (project convention: one dir per task) and RUN it — it must fail for the RIGHT reason
  (missing implementation), evidence pasted into PLAN.md §4.
- Leave §3 `Status: DRAFT`. NEVER run `add.py freeze | advance | gate | use` — the freeze is the
  human's gate; the orchestrator presents your card.
- End with the freeze card content (banner → ARC → SHAPE → SUMMARY → FLAGS lowest-confidence-first →
  DECIDED → EVIDENCE → APPROVE → NEXT) as your final report.

## Locked decisions (Tin, 2026-07-24 — do NOT reopen)
- **Storage substrate = pgvector** (vector-store-core owns it). ANN index; recall/latency at scale.
  The freeze MUST: (1) add the pgvector extension to EVERY deploy target — the gateway image, the e2e
  compose (`infra/docker-compose.e2e.yml`), and the kind stack — with a migration that `CREATE EXTENSION
  IF NOT EXISTS vector`; (2) choose the embedding dimension + index type (ivfflat vs hnsw) and justify;
  (3) define the vector_store + vector_store_file + chunk schema. The existing float8[]+cosine
  `proxy/infrastructure/vector_cache.py` is the DISPLACED path — cite it as prior art, do not extend it
  as the store. Chunk/index shape is still yours to design.

## Hard project rules (non-negotiable)
- GROUND by live reads (serena MCP tools preferred), never memory; cite SYMBOLS not line numbers;
  tag claims [OBSERVED]/[DERIVED]/[PRIOR]/[ASSUMED].
- Byte-identical default path: a request not using your new surface engages ZERO new plumbing.
- Every proxied inference/retrieval op → exactly ONE billing record: file_search bills a NEW per_query
  pricing_unit (NOT a second inference bill); fine-tune = pass-through provider cost + tenant markup via
  the ONE shared rate-card resolver; surfaces with no cost write NO usage record.
- No outbound IO without timeout + bounded retry (idempotent only) + circuit breaker.
- Tenant scoping on every row; cross-tenant reads return 404 (never a leak, never 403/404 alternation).
- New payload-at-rest stores (the vector store IS one) MUST compose with ZDR (metadata-only), the
  retention sweeper, residency, and the payload-capture inventory; new tables go in BOTH manifests
  (tests/migrations EXPECTED_TABLES + guardrails NOT-IN list).
- New /v1 routes ride the existing Envoy ext_authz gate — Envoy's `/v1/` prefix rule already covers
  them (verify in `infra/envoy/envoy.yaml`); no new Envoy route needed unless you carve an exception.
- Fine-tune BYOK credentials handled like every other BYOK seam (Fernet, never logged); the provider
  credential is a cross-tenant confused-deputy surface — fail-closed, tenant-keyed.
- Pyright strict (`make ci` runs it); TypedDict/typed extras seam — no inspect.signature dispatch.
- Tests: red/green TDD; shared test postgres :5433 needs a unique GATEWAY_TEST_DATABASE_URL;
  don't run the full suite — only your task dir's suite.

## Key anchors (verify live before citing)
- R4 file store: `apps/gateway/src/gateway/files/` (orm, repository, wire_id.py `file-<hex>↔UUID`, api/router.py) — the RAG/fine-tune INPUT source.
- Responses host: `apps/gateway/src/gateway/responses_store/` + `proxy/api/responses_router.py` — where file_search attaches as a server-side tool.
- Embeddings seam: the chunk→embed path + ChatTranslator provider seam (openrouter-embeddings milestone).
- float8[] prior art (DISPLACED): `memory/infrastructure/orm.py` (DOUBLE PRECISION[]) + `proxy/infrastructure/vector_cache.py` (cosine).
- Catalog: `catalog/` ModelRow + pricing_snapshots — finetune-model-registry registers the resulting model here.
- Pricing dispatch: the `pricing_unit` dispatcher — add a per_query unit for file_search.
- Job-store precedent: `batches/` (api/router.py, infrastructure/orm.py) — the fine-tune job store mirrors this shape.
- Usage ledger: `usage/` (usage_records, recorder) + the `/v1/organization/costs|usage/completions` read API (R4).

## Sibling wave tasks (do not build their scope; name the seam if you touch it)
wave-1 (freeze-first, parallel): vector-store-core ∥ finetune-broker.
wave-2: vector-store-files (depends vector-store-core) ∥ finetune-model-registry (depends finetune-broker).
wave-3: file-search-tool (depends vector-store-files).
vector-store-core: design the store schema so vector-store-files can attach chunks WITHOUT re-freezing
the core contract, and so file-search-tool can query top-k WITHOUT a schema change — note the extension
points, don't build them.
finetune-broker: design the job-store + brokering contract so finetune-model-registry can register the
resulting model WITHOUT re-freezing the broker contract.

## Report discipline
Self-score (6 dims, refine weakest <0.9 before reporting). Your final message = the freeze card,
nothing else load-bearing outside it.
