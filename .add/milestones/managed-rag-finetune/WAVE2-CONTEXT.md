# WAVE-2 shared context — managed-rag-finetune direction drafting (2026-07-24)

Orchestrator-owned. Read FIRST. Do NOT edit this file, MILESTONE.md, state.json, or any other task's files.

## Mission
Milestone `managed-rag-finetune` (R5, feeds 0.13.0). Wave-1 (vector-store-core ∥ finetune-broker)
is DONE + gated PASS — their §3 contracts are FROZEN and their code is committed on this branch.
Wave-2 builds ON TOP of them. Full scope + Ground: `.add/milestones/managed-rag-finetune/MILESTONE.md`.

## Your beat: DIRECTION only (draft the whole bundle; do NOT freeze)
- Phase guide: `/Users/tindang/.claude/skills/add/phases/direction.md` (read it; §1 → §3 → §4).
- Draft the WHOLE bundle in ONE write into YOUR task's `.add/tasks/<your-slug>/PLAN.md` (scaffolded there).
- Write the §4 red suite as REAL tests under `apps/gateway/tests/<your_slug_underscored>/` and RUN it —
  it must fail for the RIGHT reason (missing implementation), evidence pasted into §4. Use a unique
  GATEWAY_TEST_DATABASE_URL=postgresql+asyncpg://gateway:gateway@localhost:5433/gateway_test_<slug>
  (CREATE the DB first: `docker exec hydroa-dev-postgres-1 psql -U gateway -d postgres -c "CREATE DATABASE gateway_test_<slug>"`).
  Run pytest with `--override-ini="addopts="` to skip the repo coverage gate. macOS has NO `timeout`.
- Leave §3 `Status: DRAFT`. NEVER run `add.py freeze | advance | gate | use`.
- End with the freeze card (banner → ARC → SHAPE → SUMMARY → FLAGS lowest-confidence-first → DECIDED
  → EVIDENCE → APPROVE → NEXT) as your final report.

## The FROZEN wave-1 contracts you build on (do NOT re-freeze; consume the named extension points)
- **vector-store-core §3 (FROZEN):** tables `vector_stores`, `vector_store_files` (UNIQUE(store,file),
  a `status` column for ingestion state), `vector_store_chunks(embedding vector(1536) NOT NULL, HNSW
  cosine, tenant_id denormalized)`. Module `apps/gateway/src/gateway/vector_stores/` (orm, repository,
  api/router, wire_id `vs_<32hex>`). Extension point: wave-2 ATTACHES chunks + sets file status with
  ZERO DDL / ZERO re-freeze. Per-store `embedding_model`/`embedding_dim` columns exist (dim never on
  the wire). **`raise_if_zdr` MUST be the first line of the chunk-write path** (the first payload-at-rest
  chunk write — vector-store-core explicitly deferred this to you).
- **finetune-broker §3 (FROZEN):** tables `finetune_jobs` (nullable `fine_tuned_model`), `finetune_job_events`.
  Module `apps/gateway/src/gateway/finetune/`. Extension point: an optional `finetune_completion_listener`
  port invoked AFTER the terminal CAS commit (D4 heal — listener exceptions are caught, never roll back
  the CAS). finetune-model-registry WIRES that listener to register the model; do it WITHOUT re-freezing
  the broker contract. Per-tenant breaker + tenant_id-threaded provider port already in place.

## Hard project rules (non-negotiable)
- GROUND by live reads (serena MCP preferred), never memory; cite SYMBOLS; tag [OBSERVED]/[DERIVED]/[PRIOR]/[ASSUMED].
- Byte-identical default path: a request not using your surface engages ZERO new plumbing.
- Billing: file_search's per_query unit is WAVE-3 (not yours); vector-store-files ingestion may meter
  embeddings if it calls the embeddings provider — exactly ONE usage record per billable op on the
  served model via the pricing_unit dispatcher; a fine-tuned model registered by finetune-model-registry
  MUST price + bill through the ONE shared rate-card resolver (no new billing mechanism).
- No outbound IO without timeout + bounded retry (idempotent only) + circuit breaker — and the breaker
  MUST be per-tenant (this repo has HARD-STOPPED on a shared breaker twice: moderations CR-1, finetune D1).
- Tenant scoping on every row; cross-tenant reads return 404 (never a leak, never 403/404 alternation).
- New payload-at-rest (chunk embeddings) composes with ZDR (metadata-only), retention sweeper, residency,
  payload-capture inventory; any NEW table goes in BOTH manifests (tests/migrations EXPECTED_TABLES +
  guardrails NOT-IN). Chain your migration to the CURRENT head `6f2a9c1e3b7d`.
- Pyright strict; TypedDict/typed extras seam — no inspect.signature dispatch.

## Key anchors (verify live before citing)
- vector-store-core built code: `apps/gateway/src/gateway/vector_stores/` + migration `55dc3f920a38`.
- finetune-broker built code: `apps/gateway/src/gateway/finetune/` + migration `6f2a9c1e3b7d`.
- R4 files store (input source): `apps/gateway/src/gateway/files/` (file_id resolution, wire_id).
- Embeddings seam (chunk→embed): the openrouter-embeddings path + ChatTranslator provider seam.
- Catalog: `apps/gateway/src/gateway/catalog/` — ModelRow + pricing_snapshots (finetune-model-registry
  registers the fine-tuned model here); the shared rate-card resolver.
- Usage ledger: `apps/gateway/src/gateway/usage/` (usage_records, recorder).
- ZDR gate: `raise_if_zdr` (used in files/responses payload-at-rest paths).

## Sibling wave tasks (do not build their scope; name the seam if you touch it)
wave-2 (parallel, YOU): vector-store-files (dep vector-store-core) ∥ finetune-model-registry (dep finetune-broker).
wave-3 (NOT yours): file-search-tool (dep vector-store-files) — design your chunk schema/status so top-k
retrieval can query WITHOUT a re-freeze; note the extension point, don't build it.

## Report discipline
Self-score (6 dims, refine weakest <0.9 before reporting). Final message = the freeze card only.
