"""add_engine — the ADD engine, modularized.

add.py is the runnable entry; the engine body lives here as focused modules.
Started by the engine-modularization milestone (first module: constants).
"""
