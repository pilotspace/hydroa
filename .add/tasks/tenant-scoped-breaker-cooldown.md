---
type: Task
title: Tenant-scope every breaker and the cooldown gate on the tenant-reachable proxy paths
status: done
depth: deep
sensitivity: security
milestone: release-hardening-p0
scope:
  - apps/gateway/src/gateway/proxy
  - apps/gateway/src/gateway/usage/application/cost_recovery.py
  - apps/gateway/src/gateway/observability/metrics.py
  - apps/gateway/tests
gives:
  - S1 `TenantScopedBreakerRegistry.get_or_create(tenant_key) -> CircuitBreaker` — the ONE shared bounded-LRU per-tenant breaker registry the whole proxy uses
  - S2 `breaker_tenant_key() -> object` — resolves the tenant key a breaker lookup is scoped by, from the same request context that supplied the credential
  - S3 `ProviderScopedCircuitBreakerUpstream._breaker_for(payload) -> CircuitBreaker` — the /v1 chat wrapper breaker, keyed per (tenant, provider)
  - S4 `TenantScopedBreakerMixin._breaker_for() -> CircuitBreaker` — the single accessor every proxy upstream adapter inherits in place of its own `self._breaker`
  - S5 `ModelHealthGate.is_available(model_id, tenant_id) -> bool`
  - S6 `ModelHealthGate.record_failure(model_id, tenant_id) -> None`
  - S7 `ModelHealthGate.record_success(model_id, tenant_id) -> None`
  - S8 `RedisCooldownGate` tenant-partitioned key shape `gateway:cooldown:{kind}:{tenant_key}:{model_id}`
  - S9 `RedisCooldownGate.snapshot_state(model_id, tenant_id) -> str`
  - S10 `GET /admin/routing` — the superadmin `candidates[].state` read over tenant-partitioned cooldown state
  - S11 `proxy/api/realtime_ws.py` chat-turn upstream — resolves its breaker from S1 instead of `app.state.circuit_breaker`
  - S12 a structural guard that fails if any module under `proxy/` constructs a `CircuitBreaker` outside the registry module
  - S13 a boot-time census that fails if any live `CircuitBreaker` reachable from the booted app is not owned by a `TenantScopedBreakerRegistry`
  - S14 `gateway_circuit_breaker_state` — the Prometheus gauge, re-pointed at the tenant registries so it reports a breaker production actually drives
generated: { by: add/3.2.0, at: 2026-08-18 }
verified:
  - { by: "Tin Dang", at: 2026-08-19, act: freeze, authority: human, direction: "sha256:e4402f171608d1a8" }
  - { by: "cli", at: 2026-08-19, act: brief, authority: process, brief: "sha256:7f8912908041477a" }
  - { by: "cli", at: 2026-08-20, act: brief, authority: process, brief: "sha256:3e6cae05a1621a9e" }
  - { by: "Tin Dang", at: 2026-08-20, act: refreeze, authority: human, direction: "sha256:10809d8054848a5e" }
  - { by: "cli", at: 2026-08-20, act: brief, authority: process, brief: "sha256:3e6cae05a1621a9e" }
  - { by: "process:run", at: 2026-08-20, act: run, authority: process, outcome: PASS, receipt: /tasks/tenant-scoped-breaker-cooldown.d/runs/1.md }
  - { by: "Tin Dang", at: 2026-08-20, act: gate, authority: human, outcome: PASS, receipt: /tasks/tenant-scoped-breaker-cooldown.d/runs/1.md, brief: "sha256:0491567f8f2a2797" }
advised_by: appsec-engineer
---
## CARD
goal: One tenant's upstream failures can never deny service to another tenant — on the /v1 chat path, in every proxy upstream adapter, through the Redis cooldown gate, and on the realtime websocket path — with a structural guard that closes the class so this cannot recur a fourth time.
why: The 2026-08-18 deep review filed this as P0 #3 citing two sites; BOTH citations are imprecise and the review MISSED the real exposure. `proxy/api/deps.py:141` is keyed per PROVIDER (audit-remediation C1 already de-globalized it) — tenant-blind, but not "global". `proxy/infrastructure/redis_cooldown_gate.py:36-39` IS tenant-blind and cross-replica, but is DEFAULT-OFF (`core/config.py:944` `cooldown_failure_threshold: int = Field(default=0)`; `main.py:1548` builds the gate only when > 0). The actual live cross-tenant DoS is one layer DOWN: ELEVEN process-wide adapter breakers under `proxy/` (`infrastructure/openrouter_upstream.py:133`, `infrastructure/anthropic_upstream.py:872`, `infrastructure/vertex_upstream.py:180`, `infrastructure/gemini_upstream.py:822`, `infrastructure/gemini_upstream.py:1030`, `infrastructure/openai_provider.py:85`, `infrastructure/bedrock_upstream.py:590`, `infrastructure/azure_upstream.py:83`, `infrastructure/azure_embeddings.py:88`, `infrastructure/bedrock_embeddings.py:120`, `application/realtime_relay_pump.py:80`), each `self._breaker = CircuitBreaker()` in `__init__`, each hard-refusing via `self._breaker.guard()` (e.g. `openrouter_upstream.py:317`), each constructed ONCE in `create_app()` (`main.py:1236`, `main.py:1287-1289`) while carrying PER-TENANT BYOK credentials resolved per-request from a contextvar (`main.py:1282-1286`). NOT a twelfth site: `ml_moderation_evaluator.py:151` is the construction INSIDE `_TenantBreakerRegistry.get_or_create` — already per-tenant, and this task's promotion source, not its target. The two layers also COMPOUND: `deps.py:155` catches `CircuitOpenError` from the adapter and counts it as the wrapper's own failure, so one tenant's adapter trip drives the wrapper's shared breaker toward opening too. Tenant A's own upstream 5xx/timeouts therefore trip a breaker that refuses every OTHER tenant on that provider. This defect class has already been HARD-STOPPED at a verify gate TWICE (residency-service-tiers, api-surface-parity R4) and fixed once in one seam only (moderations CR-1, Tin HARD-STOP 2026-07-24) — the remedy primitive already exists, private, at `ml_moderation_evaluator.py:111` `_TenantBreakerRegistry`. This task promotes that primitive and applies it everywhere, because fixing today's two cited sites would leave the live DoS untouched.
beat: done · next: add status

## RULES
<must>
- M1 On the /v1 chat path, tenant A driving enough consecutive upstream failures to OPEN the breaker MUST NOT cause tenant B's request to be refused. Proven by driving REAL failures (statuses that actually trip: `circuit_breaker.py:47` counts 408, 429 and >= 500; a 401/403 does NOT trip) past the threshold of 5 (`circuit_breaker.py:25`) and then serving tenant B.
- M2 The adapter-level breaker is per-tenant at EVERY BREAKER ENTRY POINT, not merely once per adapter. The AST-derived population is THIRTY-ONE entry points across fourteen classes (30 distinct source lines — `deps.py:177` is reached from both `stream` and its nested `_gen`) — eleven breaker-owning adapters is the count of OWNERS, never the count of entry points, and the difference is where this defect hides. An entry point is any function reaching `breaker.guard()`, `breaker.call_allowed()`, or `execute_with_retry(...)` (which guards per ATTEMPT at `upstream_retry.py:137`). `OpenAIDirectProvider` alone has SIX (`complete:163`, `stream:181`, `post_json:223`, `post_json_with_retry:281`, `post_multipart:303`, `stream_bytes:334`); `AzureOpenAIProvider` has three; `OpenRouterCompletionUpstream` has four. `BoundCircuitBreakerUpstream` (`circuit_breaker_proxy.py:42`, `:64`) is a fourteenth entry-point class that CONSTRUCTS no breaker — it receives one — so a construction-site census alone would never have seen it, and it is the very wrapper the realtime path uses (M8).
- M13 `breaker_tenant_key()` MUST resolve in precedence order: the credential tenant (`current_credential_tenant`), ELSE the guardrail tenant (`guardrail_tenant_context`), ELSE the sentinel. Reading only the credential tenant would REGRESS a shipped security fix: the moderation seam is already per-tenant, but it keys off `get_guardrail_tenant_id()` (`ml_moderation_evaluator.py:270`, `:389`) while deliberately setting its credential UNTAGGED (`:402`), so a credential-only resolver would collapse every tenant's moderation traffic into one sentinel bucket and silently undo the CR-1 HARD-STOP fix.
- M11 No caller may reach a breaker with an untagged credential when it HAS a tenant in hand. `usage/application/cost_recovery.py:168` calls `set_provider_credential(cred)` with no tenant while `tenant_id` sits in scope one line above (`:165`), from a background task, against the SAME adapter instance the live chat path uses (`main.py:1727`) — so today it silently accumulates failures that can open a breaker shared with real tenant traffic. A second untagged site is `ml_moderation_evaluator.py:402`.
- M3 `ProviderScopedCircuitBreakerUpstream` keys its registry per (tenant, provider), not per provider alone. `app.state.provider_circuit_breakers` changes key shape accordingly.
- M4 There is exactly ONE registry implementation in the tree. The private `_TenantBreakerRegistry` at `ml_moderation_evaluator.py:111` is REPLACED by the shared S1 primitive (imported, not forked, not kept alongside) — a second implementation is how the two halves drift apart, and a redundant duplicate would mask S12.
- M5 The per-tenant registry is BOUNDED. Tenant count is unbounded and attacker-influenceable (self-serve signup, free tier), so an uncapped dict keyed by tenant is a memory-growth vector. Bounded LRU, cap documented, and eviction MUST target only the least-recently-used entry so a hot tenant's OPEN breaker survives.
- M6 The cooldown gate is tenant-partitioned on BOTH halves: the `ModelHealthGate` port carries the tenant (S5, S6, S7) and the Redis keys carry it (S8). Partitioning only the port would leave every replica still sharing one Redis key per model.
- M7 `GET /admin/routing` still returns a coherent, honestly-labelled `candidates[].state` after partitioning. It is a `require_superadmin` route with NO tenant in scope (`routing_admin_router.py:167`), so a per-model state no longer has a single answer — it must not silently report "closed" over a partition it did not inspect.
- M8 The realtime websocket chat turn resolves its breaker per-tenant. `realtime_ws.py:329` reads `app.state.circuit_breaker` — the single legacy global breaker shared by every tenant. `main.py:1223-1230` openly declares it deferred; this is the third time it would be inherited.
- M9 A structural guard fails if any module under `apps/gateway/src/gateway/proxy/` constructs a `CircuitBreaker` outside the registry module, so the next provider surface cannot ship a tenant-blind breaker silently. Its population is DISCOVERED from the AST, never hand-enumerated.
- M12 The class is closed by TWO guards, not one. The static AST census (M9) is evadable by construction — an import alias, `_CLS = CircuitBreaker`, a module-level default argument, `functools.partial`, or a subclass all produce a tenant-blind breaker without a literal `CircuitBreaker(` call in that module. A BOOT-TIME construction census complements it: build the real app and assert every live `CircuitBreaker` instance reachable from `app.state` and the provider registry is owned by a `TenantScopedBreakerRegistry`. Neither guard alone is sufficient; the static one catches source that never boots, the runtime one catches evasion.
- M10 Breaker and gate SEMANTICS are otherwise unchanged: same threshold, same cooldown, same fail-open contract on the gate (`redis_cooldown_gate.py:117-123`), same zero-Redis-command fast path when `threshold == 0`, no new outbound IO, no new await on the hot path. Isolation is achieved by partitioning the key, never by weakening or removing protection.
- M14 This task must not leave the operator a metric that reports health over a surface nothing drives. `observability/metrics.py:184` sets `gateway_circuit_breaker_state` from `app.state.circuit_breaker`; once M8 moves the realtime path off that breaker, NOTHING in production moves it (verified: zero live `guard()` / `on_upstream_error()` / `record_success()` call sites against it), so the gauge is pinned at `0.0` = "closed" forever. The gauge MUST be re-pointed at the tenant registries and report an honest aggregate — this is A18's rule (never report "closed" over a partition you did not inspect) applied to the Prometheus surface instead of the admin board. The AST census (M9) is structurally blind to this because `metrics.py` sits OUTSIDE `proxy/`.
</must>
<reject>
- R:SHARED_BUCKET an unattributed call (no tenant resolvable in context) sharing a breaker or cooldown bucket with a real tenant — that is the same cross-tenant coupling wearing a different key -> "SHARED_BUCKET"
- R:GLOBAL_BREAKER any breaker on a tenant-reachable proxy path that remains process-wide after this task, including a new one added later -> "GLOBAL_BREAKER"
- R:UNBOUNDED a per-tenant registry with no size cap — trading a cross-tenant DoS for a memory-exhaustion DoS is not a fix -> "UNBOUNDED"
- R:HOT_EVICT an eviction policy that can reset an actively-failing tenant's OPEN breaker back to CLOSED, removing the upstream protection the breaker exists to provide -> "HOT_EVICT"
- R:WEAKEN achieving "isolation" by disabling, bypassing or fail-opening the breaker rather than partitioning it — every tenant unprotected is not every tenant isolated -> "WEAKEN"
- R:KEY_COLLISION a Redis cooldown key shape in which a caller-controlled `model_id` can reach another tenant's partition. `model_id` comes straight from the request body and may contain `:`, so the tenant segment MUST precede it and the key MUST NOT be assemblable into another tenant's namespace -> "KEY_COLLISION"
</reject>

## ASSUMPTIONS
- A1 [who] covers: S1, S2, S3, S4, S11 · the request does not say whose identity scopes a breaker; taking: the Hydroa TENANT that owns the credential the call is authenticated with — not the API key, not the end user, not the provider account — because the tenant is the billing and isolation boundary the product sells and the unit a DoS victim is measured in · probe: two API keys of the SAME tenant share one breaker while two tenants never do -> if wrong, a per-key breaker would fragment protection and let one tenant hammer an upstream with N keys
- A2 [who] covers: S5, S6, S7, S8, S9 · the request does not say whose identity partitions the cooldown gate; taking: the same tenant already threaded into `FallbackModelRouter.complete` for residency filtering (`fallback_router.py:251` `tenant_id: uuid.UUID | None`, used at `:311`) — the gate call sites at `:333`, `:370`, `:399`, `:414` sit inside that same scope, so the tenant is already in hand and no new plumbing is required · probe: the gate calls receive the router's own `tenant_id` argument, not a contextvar read -> if wrong, the port change is wider than the router
- A3 [who] covers: S10 · the request does not say who reads the partitioned cooldown state; taking: a platform SUPERADMIN with no tenant of their own (`routing_admin_router.py:167` `Depends(require_superadmin)`) — so this surface reads ACROSS partitions and must never be re-scoped to a single tenant silently -> if wrong, an admin sees one arbitrary tenant's circuit state and believes it is the fleet's
- A4 [who] covers: S12, S13 · n/a · the guard is a test — it acts as no principal
- A5 [which] covers: S2 · the request does not say where the tenant key comes from at an adapter, which has no tenant argument and cannot gain one without changing `CompletionUpstream` for twelve implementations; taking: the EXISTING `current_credential_tenant` contextvar (`proxy/domain/credential_context.py:43`), set at the single credential-resolution seam via `set_provider_credential(cred, tenant_id)` (`use_cases.py:1355`, and `:1389` for the platform-key fallback) — the same channel that already supplies these adapters their per-request credential, and the same channel a prior cross-tenant SECURITY fix used (the vertex BYOK token-cache confused-deputy, documented at `credential_context.py:37-42`) · found: the contextvar is the right channel, but the tempting invariant "no tenant in context ⇒ no credential ⇒ never dials" is FALSE — `usage/application/cost_recovery.py:168` sets a credential with NO tenant from a background task and reaches `guard()` on the same adapter instance the chat path uses (`main.py:1727`), and `_auth_headers` raises ProviderKeyMissing only INSIDE the request, after `guard()` has already run (`openrouter_upstream.py:186-196` vs `:317`). So the untagged bucket is REACHABLE and A16's sentinel is load-bearing, not defensive · probe: an adapter breaker lookup and the credential it guards resolve from the same context in the same call -> if wrong, the tenant must be threaded through the port and the diff grows by eleven adapter signatures
- A6 [which] covers: S4 · the request does not say whether each adapter gets its own registry or all share one; taking: one registry PER ADAPTER INSTANCE, so anthropic's failures never touch openrouter's — preserving the per-provider isolation `deps.py` already won (audit-remediation C1) while adding the tenant dimension underneath it · probe: a trip on one adapter leaves a different adapter instance's breaker CLOSED, mirroring the invariant `test_cr1_moderation_breaker_still_isolated_from_completion_breaker` already holds for moderation -> if wrong, one shared registry would re-couple providers, undoing a shipped fix
- A7 [which] covers: S1 · the request does not define the registry's implementation; taking: PROMOTE the already-shipped `_TenantBreakerRegistry` (`ml_moderation_evaluator.py:111`) verbatim into a shared module rather than writing a second one — it was designed under a Tin HARD-STOP, its LRU-over-FIFO reasoning is documented in place, and it already carries a bounded cap and a passing eviction suite · probe: `ml_moderation_evaluator` imports the shared primitive and defines no registry of its own -> if wrong, two registries drift and M4 is violated by construction
- A8 [which] covers: S12, S13 · the request does not define the guard's population; taking: the guard DISCOVERS it by walking the AST of every module under `proxy/` for a call to `CircuitBreaker(`, and flags every such call outside the registry module — a rule that IS the hazard rather than a list that goes stale. Deliberately narrow: it asks only "who constructs one", which is exactly the act that creates a new failure domain · probe: the guard is RED against the pre-fix tree and its discovered population there is EXACTLY the twelve adapter sites plus `deps.py:141` -> if wrong, the population widens without changing the predicate's shape
- A9 [which] covers: S11 · the request does not say whether the realtime path is in scope; taking: IN — `main.py:1223-1230` deferred it explicitly once and the review's framing would defer it again; a knowingly-deferred cross-tenant DoS on a tenant-reachable path is the same finding, and routing it through S1 is the same one-line change as every adapter -> if wrong, cutting it is a scope decision Tin can make at freeze, and it is isolated to one file
- A10 [when] covers: S3, S4 · the request does not say at which moment in a call the tenant key is read; taking: EAGERLY, at the same instant the breaker is resolved for the guard — never lazily inside a generator body. `stream()` calls `guard()` eagerly but yields lazily (`openrouter_upstream.py:317` vs the `_gen()` body at `:320-350`), and `use_cases.py:2246-2260` documents that the credential contextvar is reset by its own `finally` before deferred work runs — so a lazily-read tenant would be correct at guard time and None at record time, splitting ONE request across two buckets · probe: a streamed response records its success or failure against the SAME tenant bucket that was guarded -> if wrong, streaming traffic silently accumulates failures in the unattributed bucket
- A11 [when] covers: S1, S2 · the request does not say when a tenant's breaker is created; taking: lazily on first use, never pre-seeded per tenant — pre-seeding would make the registry grow with the tenant table rather than with active traffic -> if wrong, memory tracks total tenants instead of concurrent ones
- A12 [when] covers: S5, S6, S7, S8, S9 · the request does not say when the tenant partition applies relative to the `threshold == 0` fast path; taking: AFTER it — `threshold == 0` still returns True with zero Redis commands before any key is built (`redis_cooldown_gate.py:85-86`), so the default-off deployment is byte-identical and pays nothing · probe: with the gate disabled, the partitioned build issues zero Redis commands -> if wrong, a default-off feature starts doing IO, which is a regression for every deployment that never enabled it
- A13 [when] covers: S10 · n/a · the admin read has no boundary condition of its own; it reflects whatever the partitioned keys hold at read time
- A14 [when] covers: S11 · the request does not say when the realtime path resolves its tenant; taking: at the same point it already resolves the authenticated identity for the chat turn, before `BoundCircuitBreakerUpstream` is constructed (`realtime_ws.py:329-334`) -> if wrong, the websocket turn falls into the unattributed bucket, which E2 already forbids from sharing with a real tenant
- A15 [when] covers: S12, S13 · n/a · the guard runs at CI collection time; it has no runtime boundary
- A16 [absent] covers: S1, S2, S3, S4, S11 · the request does not say what a call with NO resolvable tenant does; taking: a single reserved sentinel bucket that is shared ONLY among unattributed calls and never with any real tenant (R:SHARED_BUCKET), rather than a fresh breaker per call — a fresh-per-call breaker would remove upstream protection entirely and hammer a failing provider without bound, violating M10. The house precedent for an unknown owner is `get_credential_tenant`'s own documented rule (`credential_context.py:128-135`): never share an entry across an unknown owner · probe: an unattributed call cannot open, and cannot be blocked by, any real tenant's breaker -> if wrong, the unattributed path either loses protection or re-creates the coupling this task closes
- A17 [absent] covers: S5, S6, S7, S8, S9 · the request does not say what the gate does when `tenant_id` is None (the router already types it `uuid.UUID | None`); taking: the same reserved sentinel partition as A16, never a bare unprefixed key — a bare key would be the CURRENT global key, i.e. the defect surviving as the None case · probe: a None-tenant gate call writes into the sentinel partition, not the legacy unprefixed key -> if wrong, the pre-fix key shape lives on as a shared bucket
- A18 [absent] covers: S10 · the request does not say what the admin surface shows for a model with no cooldown entry in ANY partition; taking: `"closed"` as today, but a state that is open in SOME partition must not be reported as `"closed"` — the honest aggregate is "open somewhere" (M7). A silent "closed" over an uninspected partition is the masked-gate failure mode ([[masked-gate-never-reached-a-verdict]]) -> if wrong, an operator reads a green board over a live open circuit
- A19 [absent] covers: S12, S13 · the request does not say what the guard does with a module it cannot parse; taking: FAIL — an unparseable module under `proxy/` is a finding, never a skip -> if wrong, a false positive is one named exemption
- A20 [order] covers: S8 · the request does not say how the tenant and model segments are ordered in the Redis key; taking: TENANT FIRST, `model_id` LAST — `model_id` is caller-controlled (it arrives in the request body) and may contain `:`, so any shape that places it before the tenant segment lets a crafted model id address another tenant's partition. Tenant-first makes the collision unreachable by construction rather than by validation · probe: a model id containing `:` cannot read or write any other tenant's cooldown key -> if wrong, this is a tenant-reachable cross-tenant write, which is a HARD-STOP not a bug
- A21 [order] covers: S3, S4 · the request does not say what orders the two breaker layers; taking: unchanged — the `deps.py` wrapper still guards before delegating and the adapter still guards inside; both simply consult a tenant-keyed registry instead of a shared instance. Both must be partitioned because they are independent failure domains in series: partitioning only the outer leaves the inner live, and a partitioned outer would MASK an unpartitioned inner in any test that only drives the outer -> if wrong, one layer silently keeps the DoS
- A22 [order] covers: S1 · the request does not say what breaks a tie when the registry is at capacity; taking: strict LRU with move-to-end on every access (hit or miss), so the eviction victim is always the least-recently-used entry and a tenant driving traffic is by construction never it · probe: a hot tenant's OPEN breaker object survives an insertion burst far past the cap -> if wrong, a churn of new tenants could reset a failing tenant's breaker and remove upstream protection
- A23 [order] covers: S5, S6, S7 · the request does not say where `tenant_id` sits in the port signature; taking: appended as a keyword-only parameter after `model_id`, so the three methods stay uniform and every fake gate in the test tree fails LOUDLY on the signature change rather than silently accepting a positional it ignores -> if wrong, a fake that swallows `**kwargs` would keep passing while testing the old shape
- A24 [order] covers: S2, S9, S10, S11, S12, S13 · n/a · these surfaces have no ordering or tie-break decision — each resolves a single value or reaches a set-membership verdict
- A25 [experience] covers: S10 · the request does not say what the operator sees; taking: the superadmin routing board keeps its existing shape but a state that is open for any tenant is labelled honestly rather than averaged into "closed", so an operator debugging an outage is not told the circuit is fine while a tenant is being refused · probe: with one tenant's circuit open, `GET /admin/routing` does not report that model as plainly `"closed"` -> if wrong, the board actively misleads during exactly the incident it exists for
- A26 [experience] covers: S12, S13 · the request does not say what a failure tells the next engineer; taking: the message names the file, the line and the registry the construction should have gone through, and says why (cross-tenant DoS, HARD-STOPPED three times) -> if wrong, the guard still fails, just ruder
- A27 [experience] covers: S1, S2, S3, S4, S5, S6, S7, S8, S9, S11 · the request does not say what the END TENANT experiences; taking: NOTHING changes for a well-behaved tenant — same statuses, same latency, no new error surface; the only observable difference is that a tenant who would previously have been refused because of a NEIGHBOUR's failures is now served · probe: the existing proxy suites pass unchanged, and a tenant with a healthy upstream is served while a neighbour's circuit is open -> if wrong, a new user-visible status code would itself be the regression
- A28 [which] covers: S10 · the request does not say whether the admin surface gains a tenant filter; taking: NO new query parameter in this task — the aggregate answer (M7/A18) is what an operator needs and a per-tenant drill-down is a separate product decision -> if wrong, the drill-down is additive later and breaks nothing
- A30 [which] covers: S3 · the request does not say which requests the wrapper's partition covers; taking: every request that resolves through `get_completion_upstream` — `/v1/chat/completions` and the `/v1/messages` ingress alike — because both share the one `app.state.provider_circuit_breakers` registry today and excluding either would leave a partitioned path next to an unpartitioned one on the same key space · probe: both ingress shapes resolve a breaker under the same (tenant, provider) key -> if wrong, one ingress keeps a shared failure domain and the isolation claim is false for it
- A31 [which] covers: S5, S6, S7, S8, S9 · the request does not say WHICH of the gate's four key kinds are partitioned; taking: ALL FOUR — `fails`, `open`, `half` and `probe` (`redis_cooldown_gate.py:36-39`). Partitioning only the `open` key would leave the shared `fails` COUNTER intact, so tenant A's failures would still accumulate toward a trip that opens tenant B's circuit — the defect surviving in the one key that actually causes it. The `probe` key is equally load-bearing: shared, it lets one tenant claim the single half-open probe token and force every other tenant to be refused (`redis_cooldown_gate.py:103-111`) · probe: all four key kinds carry the tenant segment, asserted on the actual key strings issued to Redis -> if wrong, the gate looks partitioned while the trip mechanism stays shared
- A32 [who] covers: S14 · the request does not say whose state the gauge speaks for; taking: the OPERATOR, fleet-wide, with NO tenant dimension in the label set — a `tenant` label would be unbounded and attacker-influenceable through self-serve signup, which is the exact argument M5/R:UNBOUNDED makes about the registry, reappearing as Prometheus cardinality explosion. The tenant dimension is what the gauge AGGREGATES OVER, never what it labels by · probe: the exported series carry no tenant-identifying label and their count is bounded by the provider/adapter set -> if wrong, this task trades a cross-tenant DoS for a metrics-cardinality DoS, which is R:UNBOUNDED wearing a different hat
- A33 [which] covers: S14 · the request does not say which breakers the gauge reads once the single global one is dead; taking: the SAME live population S13's boot census walks — every `CircuitBreaker` owned by a `TenantScopedBreakerRegistry` reachable from the booted app — so the gauge and the census can never disagree about what exists · probe: a breaker the boot census counts is a breaker the gauge can see -> if wrong, the gauge inspects a subset and reports "closed" over the remainder, which is the very defect M14 exists to close
- A34 [when] covers: S14 · the request does not say when the aggregate is computed; taking: at SCRAPE time, in the same collector callback that sets the gauge today (`metrics.py:184`) — never on the request hot path, because M10 forbids new work there and a breaker state read must not cost a tenant latency · probe: no proxy request path gains a metrics call -> if wrong, an observability change becomes a hot-path regression
- A35 [absent] covers: S14 · the request does not say what the gauge reports before any breaker exists (registries start empty and populate lazily per A11); taking: `0.0` = "closed", as today — an empty registry means nothing has failed, which is honestly closed, and is NOT the same as the masked "closed" M14 forbids (that one is closed over an UNINSPECTED partition, this one is closed over an inspected and genuinely empty set) · probe: a freshly booted app with no traffic exports the gauge at 0.0 rather than omitting it or reporting unknown -> if wrong, every cold start pages an operator
- A36 [order] covers: S14 · the request does not say how many partitions' states collapse into one number; taking: WORST-STATE-WINS (open > half-open > closed), the same honesty rule as A18/M7 — if any tenant's circuit is open the fleet gauge must show open, because the operator's question during an incident is "is anyone being refused", not "is the average fine" · probe: with exactly one tenant open out of many, the gauge reads open -> if wrong, a single victim tenant is averaged into invisibility during the incident the metric exists for
- A37 [experience] covers: S14 · the request does not say what the operator sees change; taking: the metric NAME and its 0/1/2 encoding are unchanged so existing dashboards and any future alert keep working — only the source of truth moves. Verified at direction that no alert rule binds this gauge today (it appears only in `metrics.py`, `tests/observability/test_observability.py` and two archived task docs), so there is no rule to break · probe: the exported metric name and value encoding are byte-identical to today's for a fleet with one open circuit -> if wrong, a rename silently breaks a dashboard that was the only consumer
- A29 [absent] covers: S3 · the request does not say what happens when `app.state.provider_circuit_breakers` is absent (the documented fail-safe at `deps.py:204-206` falls back to the legacy single breaker); taking: the fallback must ALSO be tenant-scoped, because a stripped app.state must not silently restore the global breaker this task removes · probe: the fail-safe path does not reintroduce a process-wide breaker -> if wrong, any deployment hitting the fallback keeps the defect
every `gives:` surface is swept on every dimension; `[<dim>] n/a · <why>` retires one. one line, one silence — split, never bundle. `· probe: <what shipped behavior must show>` declares a reading checkable: cite its A id from CHECKS and the gate holds the PASS to it.

## PLAN
contract: A new shared module `proxy/infrastructure/tenant_breaker_registry.py` holds `TenantScopedBreakerRegistry` (S1, promoted verbatim from `ml_moderation_evaluator.py:111` `_TenantBreakerRegistry` — bounded OrderedDict LRU, cap 4096, `get_or_create(tenant_key)`) and `breaker_tenant_key()` (S2, reading `current_credential_tenant` and returning a module-level `_UNATTRIBUTED` sentinel when unset). `TenantScopedBreakerMixin` (S4) provides one `_breaker_for()` accessor; every proxy upstream adapter replaces `self._breaker = CircuitBreaker()` with the mixin's per-instance registry, and every `self._breaker.guard()` / `.on_upstream_error()` / `.record_success()` site resolves through `self._breaker_for()` instead, with the tenant key read ONCE per call at guard time and closed over for the streaming path (A10). `ProviderScopedCircuitBreakerUpstream` (S3) keys `(tenant_key, provider)`. `ModelHealthGate` (S5, S6, S7) gains a keyword-only `tenant_id`; `FallbackModelRouter` passes the `tenant_id` it already holds; `RedisCooldownGate` builds `gateway:cooldown:{kind}:{tenant_key}:{model_id}` with the tenant segment FIRST (S8, A20) and `snapshot_state` gains the same parameter (S9). `GET /admin/routing` (S10) reports an honest cross-partition aggregate. `realtime_ws.py` (S11) resolves from S1. `ml_moderation_evaluator` imports the shared primitive and deletes its private copy (M4). The structural guard (S12) walks the AST under `proxy/` for `CircuitBreaker(` calls and permits only the registry module; the boot census (S13) uses a DISJOINT predicate — `isinstance` over the live object graph — because a runtime census sharing the static one's AST rule cannot complement it (M12). `observability/metrics.py` (S14) collects the gauge from the tenant registries with worst-state-wins aggregation and no tenant label.
strategy: red-first. Build the suite `apps/gateway/tests/tenant_breaker_isolation/` against the CURRENT tree and prove each check fails for the right reason — the cross-tenant checks must actually TRIP a breaker (5 consecutive failures with a status that counts: 408, 429 or >= 500 per `circuit_breaker.py:47`; a 401/403 will not trip and would produce a vacuous green). Template: `tests/moderations_endpoint/test_cr1_per_tenant_breaker.py` drives real failures through a real adapter over an `httpx.MockTransport` whose handler raises `httpx.ReadTimeout` — deterministic, one breaker failure per call, no retry sleep, no network. Then fix the inner adapter layer FIRST and the outer wrapper second, re-running the cross-tenant check after EACH so it is proven red against a tree where only the other layer is fixed (A21) — a partitioned outer masks an unpartitioned inner otherwise. The structural guard's predicate is tested against MUTATED SOURCE per discovered population member (ORIGINAL flagged / MUTATED clean) before it is trusted.
scope: apps/gateway/src/gateway/proxy/infrastructure/tenant_breaker_registry.py · apps/gateway/src/gateway/proxy/infrastructure/{openrouter,anthropic,vertex,gemini,bedrock,azure}_upstream.py · apps/gateway/src/gateway/proxy/infrastructure/{azure,bedrock}_embeddings.py · apps/gateway/src/gateway/proxy/infrastructure/openai_provider.py · apps/gateway/src/gateway/proxy/infrastructure/ml_moderation_evaluator.py · apps/gateway/src/gateway/proxy/infrastructure/redis_cooldown_gate.py · apps/gateway/src/gateway/proxy/application/realtime_relay_pump.py · apps/gateway/src/gateway/proxy/application/fallback_router.py · apps/gateway/src/gateway/proxy/domain/ports.py · apps/gateway/src/gateway/proxy/api/deps.py · apps/gateway/src/gateway/proxy/api/realtime_ws.py · apps/gateway/src/gateway/proxy/api/routing_admin_router.py · apps/gateway/tests/tenant_breaker_isolation/
regression floor: the existing proxy, model_fallbacks, moderations_endpoint, retry_policy, streaming_resilience and breaker_4xx_classification suites stay green — in particular `tests/proxy/test_circuit_breaker_per_provider.py` (per-PROVIDER isolation must survive the added tenant dimension, A6) and `tests/moderations_endpoint/test_cr1_per_tenant_breaker.py` (the shipped per-tenant invariant must survive the registry promotion, A7).

## EDGES
- E1 tenant A drives 5 consecutive real upstream failures and its breaker opens at BOTH layers; tenant B's next request is still served end-to-end
- E2 a call with no resolvable tenant neither opens nor is blocked by any real tenant's breaker
- E3 the registry at capacity evicts only the least-recently-used entry; a hot tenant's OPEN breaker object survives an insertion burst far past the cap
- E4 a `model_id` containing `:` cannot read or write another tenant's cooldown partition
- E5 a STREAMED response records its outcome against the same tenant bucket that was guarded, even though the generator body runs after the credential contextvar is reset
- E6 with `cooldown_failure_threshold == 0` the partitioned gate issues zero Redis commands and behaves byte-identically to today
- E7 `GET /admin/routing` does not report a model as `"closed"` while that model's circuit is open in some tenant's partition
- E8 a background cost-recovery poll, which holds a tenant but sets an untagged credential today, cannot open a breaker that serves live tenant traffic
- E10 the two SECONDARY entry points — `OpenRouterCompletionUpstream.get_generation:291` (sole caller `usage/application/cost_recovery.py:217`, inside the untagged credential scope opened at `:168`, a background task with NO request context) and the moderation entries `OpenAIDirectProvider.post_json:223` / `post_json_with_retry:281` (reached from `ml_moderation_evaluator.py:402`, also untagged) — are tenant-scoped, not dumped in the shared bucket
- E11 a tenant with only a guardrail tenant in context (moderation's shape) still gets its OWN breaker, never the sentinel
- E9 a breaker introduced by an evasion shape the static census cannot see (alias, `_CLS = CircuitBreaker`, default arg, `partial`, subclass) is still caught at boot
- E12 with exactly one tenant's circuit OPEN and every other partition closed, `gateway_circuit_breaker_state` reports open — and a freshly booted app whose registries are still empty reports closed
- E13 the four cooldown key kinds are partitioned in the ACTUAL key strings issued to Redis, and a `record_failure` for tenant A past the threshold leaves `is_available` True for tenant B on the same model

## CHECKS
- test_tenant_a_trip_does_not_deny_tenant_b_on_chat_path · covers: M1, M3, A1, A21, A27, A30, E1 · drives tenant A past the 5-failure threshold with a status that ACTUALLY trips (>= 500 / timeout, never a 401) through the real chat path, asserts A is then short-circuited without reaching the delegate, and asserts tenant B is served 200 with its own delegate call. Layer independence PROVEN at direction: with ONLY the wrapper tenant-keyed this check goes GREEN while the adapter check stays RED; with ONLY the adapter tenant-keyed the adapter check goes GREEN while this one stays RED. The two checks bypass each other's layer by construction (this one drives a fake delegate, the adapter one drives the real adapter), so neither can mask the other.
- test_adapter_breaker_is_per_tenant_not_process_wide · covers: M2, A5, A6, E1 · drives a real proxy upstream adapter over an `httpx.MockTransport` raising `ReadTimeout` (the `test_cr1_per_tenant_breaker` template) for tenant A past threshold, then asserts tenant B's call still reaches the transport. Drives ONE adapter (OpenRouter) end-to-end behaviourally as the exemplar; the other twelve breaker-owning classes are covered STRUCTURALLY by the derived census in test_no_live_breaker_escapes_the_tenant_registry_at_boot, whose population is discovered from the AST (13 classes, real names) rather than hand-typed.
- test_unattributed_call_never_shares_a_bucket_with_a_real_tenant · covers: A16, A17, E2, R:SHARED_BUCKET · BOTH halves, both behavioural. BREAKER half: an unattributed call cannot be blocked by a real tenant's open breaker, and cannot open one. GATE half (currently absent — the body touches only the registry, so this Reject is not yet earned): for every key kind, `_key(prefix, None, model)` contains the reserved sentinel segment, differs from `_key(prefix, A, model)`, and differs from the LEGACY unprefixed `f"gateway:cooldown:open:{model}"` — a bare legacy key would be the pre-fix global shape surviving as the None case, which is A17's exact failure mode.
- test_registry_is_bounded_and_never_evicts_a_hot_tenant · covers: M5, A11, A22, E3, R:UNBOUNDED, R:HOT_EVICT · fills the registry far past a small injected cap while re-touching a hot tenant whose breaker is OPEN; asserts the cap holds, the hot tenant's breaker OBJECT is identical, and it is still open.
- test_one_registry_implementation_in_the_tree · covers: M4, A7 · asserts `ml_moderation_evaluator` resolves its registry from the shared module and defines no registry class of its own — a duplicate would drift and would mask the structural guard.
- test_cooldown_gate_is_tenant_partitioned · covers: M6, A2, A12, A23, A31, E6, E13 · BEHAVIOURAL against an in-memory fake Redis (reuse `tests/cooldown_circuit/conftest.py`'s — do NOT write a third fake), so the suite stays infra-free: with threshold 3, three `record_failure(M, tenant_id=A)` leave `is_available(M, tenant_id=B)` True while `is_available(M, tenant_id=A)` is False. A signature read alone does not earn M6 — it proves the parameter exists, not that it partitions. Also asserts `threshold == 0` issues ZERO Redis commands (counted on the fake), which A12/E6 promise and nothing currently checks.
- test_cooldown_key_cannot_be_collided_by_a_crafted_model_id · covers: A20, E4, R:KEY_COLLISION · BEHAVIOURAL, on the ACTUAL KEY STRINGS the gate builds — reading prefix constants and asserting the word "tenant" appears in them is NOT sufficient and does not earn this Reject. For two distinct tenants A and B and all four key kinds: keys differ; crafted `model_id` values (`"{B}:gpt-4o"`, `"../{B}:gpt-4o"`, `":x"`, `"*"`) produce no cross-partition collision; and every key satisfies `key.startswith(fixed_prefix + str(tenant) + ":")` — the ORDER assertion, which is where A20's property actually lives and which nothing in the suite currently makes.
- test_streaming_breaker_records_against_the_guarded_tenant · covers: A10, E5 · a streamed call guarded for tenant A records its failure against tenant A's bucket even though the generator body runs after the credential contextvar has been reset; asserts the unattributed bucket stayed untouched.
- test_realtime_path_breaker_is_per_tenant · covers: M8, A9, A14 · the realtime websocket chat turn resolves a per-tenant breaker; tenant A tripping it does not refuse tenant B's turn.
- test_routing_admin_reports_an_honest_cross_partition_state · covers: M7, A3, A18, A25, A28, E7 · BEHAVIOURAL, driving the real `snapshot_state` over a fake Redis: with tenant A's `open` key seeded, `snapshot_state(M, tenant_id=None)` returns `"open"`; and when the cross-partition scan is INCOMPLETE it returns `"unknown"`, never `"closed"` — reporting closed over a partition it did not finish inspecting is the masked-gate failure mode A18 names. A signature read does not earn M7.
- test_no_direct_circuit_breaker_construction_under_proxy · covers: M9, A8, A19, A26, R:GLOBAL_BREAKER · structural guard: DISCOVERS its population by walking the AST of every module under `proxy/` for a call to `CircuitBreaker(` and fails on any outside the registry module. The carrying assertion is `assert not found` against the live tree. Its ANTI-VACUITY floor is proven against SYNTHETIC source strings (one per evasion/violation shape) parsed in memory, NEVER against the live tree — a floor of the form `assert found` over the live tree is satisfiable only WHILE the defect exists, so a correct fix would empty the population and the floor could never pass again. A separate walker-health floor asserts discovery still reaches the real tree (the module list under `proxy/` is non-trivially sized) so the guard cannot go blind by walking nothing. The predicate is proven per synthetic member against MUTATED source (ORIGINAL flagged / MUTATED clean). An unparseable module is a finding, never a skip.
- test_breaker_still_protects_each_tenants_upstream · covers: M10, A29, R:WEAKEN · partitioning did not disable protection: a single tenant driving failures still trips ITS breaker and is still short-circuited before the transport, the threshold and cooldown are unchanged, and the `deps.py` fail-safe path does not reintroduce a process-wide breaker.
- test_no_caller_reaches_a_breaker_with_an_untagged_credential · covers: M11, E8 · `cost_recovery.py:168` and `ml_moderation_evaluator.py:402` pass their tenant into `set_provider_credential`, so background cost recovery cannot accumulate failures on a bucket shared with live tenant traffic. Asserted at the call site by AST, AND behaviourally by entering the credential context the recovery path establishes and asserting `breaker_tenant_key()` resolves to that tenant rather than the sentinel — the honest form of "which bucket the failure lands in" without booting a background task.
- test_no_live_breaker_escapes_the_tenant_registry_at_boot · covers: M12, E9 · boot-time census: BUILDS THE REAL APP and walks the live object graph from `app.state` and the provider registry, collecting every object satisfying `isinstance(obj, CircuitBreaker)` and asserting each is owned by a `TenantScopedBreakerRegistry`. The population predicate MUST be `isinstance` on live objects — it may NOT reuse the static census's AST rule (`name == "CircuitBreaker"` on an `ast.Call`), because that is the very predicate M12 exists to complement: `_CLS = CircuitBreaker; _CLS()` yields `name == "_CLS"` and would silently leave the class out of the population, making the census green on four of the five evasion shapes it was written to catch. `isinstance` is invisible to alias, default-arg, `partial` and subclass alike. Anti-vacuity floor is the WALKER's reach (the traversal visits a known-nonempty set of app-state objects and terminates), never `assert owners` over the live tree — after a correct fix every breaker IS registry-owned, so a floor demanding an unowned one can never pass. The static census's blindness was proven at direction on four of five shapes; this row owns the complementary half.
- test_tenant_key_resolver_prefers_credential_then_guardrail_tenant · covers: M13, E11 · `breaker_tenant_key()` returns the credential tenant when set, the GUARDRAIL tenant when only that is set, and the sentinel only when neither is — so the moderation seam's shipped per-tenant scoping survives the promotion. RED against a resolver that reads only the credential contextvar.
- test_every_breaker_entry_point_is_tenant_scoped · covers: M2, E10 · derived census: walks the AST for every function reaching `guard()`, `call_allowed()` or `execute_with_retry(...)` under `proxy/` and asserts each resolves its breaker per tenant. Population DERIVED from the AST (31 entry points / 14 classes at direction), never hand-typed. Anti-vacuity floor is the WALKER's health, not the defect's survival: it asserts discovery still finds the known secondary entries `get_generation` and `post_json_with_retry` AS ENTRY POINTS — a fact the fix does not erase, since the fix changes how they resolve a breaker, not whether they reach one. A floor phrased as "some entry point is still unscoped" would be unsatisfiable the moment the task succeeds.
- test_breaker_state_gauge_reports_a_breaker_production_drives · covers: M14, A32, A33, A34, A35, A36, A37, E12 · the Prometheus gauge reads the tenant registries, not the orphaned `app.state.circuit_breaker`: with one tenant's breaker OPEN and every other partition closed the exported `gateway_circuit_breaker_state` reads open (A36 worst-state-wins), a freshly booted app with empty registries reads `0.0` (A35), and the exported series carry NO tenant label (A32 — a tenant label is R:UNBOUNDED as Prometheus cardinality). RED against the current tree, where the gauge is pinned at `0.0` because nothing in production drives the breaker it reads. Note for the record: `tests/observability/test_observability.py::test_metrics_breaker_state_open` PASSES today and is structurally unable to catch this — it calls `breaker.record_failure()` directly on `app.state.circuit_breaker`, so it asserts the gauge tracks that breaker (still true) and never that anything in production moves it. That test keeps a comment saying what it does not prove.
red-first: every check MUST fail first.

## EVIDENCE
receipt: pending — REFROZEN 2026-08-20 after a build-beat CHANGE-REQUEST. A first build ran against the freeze of 2026-08-19 (`sha256:e4402f171608d1a8`) and HARD-STOPPED at 14 green / 3 red, because three checks were unsatisfiable or vacuous BY CONSTRUCTION rather than because the code was wrong: (CR-1) three anti-vacuity floors were phrased over the LIVE tree (`assert found` / `assert population` / `assert owners`), and a correct fix empties those populations — a floor satisfiable only WHILE the defect exists can never pass once the task succeeds, so all three are re-based onto synthetic sources and walker-health; (CR-2) the boot census matched `name == "CircuitBreaker"` on an `ast.Call`, the SAME predicate as the static census, so M12's complementary half was not delivered — it is now an `isinstance` walk over the live object graph; (CR-3) four rows promised behaviour their bodies did not deliver (a prefix-constant read is not a key-collision proof, a `signature` read is not a partition proof), so R:KEY_COLLISION and R:SHARED_BUCKET were bound to evidence that did not exist on a `sensitivity: security` task; (CR-4) the suite file fails `ruff format --check`, which blocks `make ci`. D2 was found during the same beat and is now M14/S14. The production code from that build is retained and was independently cleared on the points that matter (all `FallbackModelRouter` call sites pass `tenant_id`; the `provider_circuit_breakers` key migration was swept for readers with no drift; `tenant_key_segment` now REJECTS a `:`-bearing key rather than applying the non-injective `replace(":", "_")`, under which `"a:b"` and `"a_b"` collided). REFUTED and recorded so it is not re-filed: a peer advisor escalated a routing-admin SCAN-glob defect (a `MATCH` pattern with a trailing `:` matching nothing, so every model reports `"closed"`) as an `[OBSERVED]` finding; it is FALSE against this tree — `_fixed_prefix` strips the trailing colon via `[:-1]`, yielding `gateway:cooldown:open:*`, which matches. Original direction-beat evidence, still standing: checks authored and proven RED against the pre-fix tree — `uv run pytest tests/tenant_breaker_isolation -q` gives 14 failed, 1 passed. The suite is deliberately INFRA-FREE (httpx.MockTransport + AST only — no Postgres, no Redis): the same 14 failed / 1 passed reproduces with the dev stack DOWN under `GATEWAY_TEST_SKIP_INFRA_CHECK=1`, so an infra dropout can never be mistaken for this suite going green ([[make-ci-infra-dropout-looks-like-regression]]). Wall time is 0.48s with the stack up; the 87s seen with it down is the global infra guard's retry, not these tests. Every one of the 12 fails on an assertion about ABSENT BEHAVIOR, never on an import or a typo — the two cross-tenant checks fail having actually TRIPPED a real breaker (5 real ReadTimeout failures dialed through an httpx.MockTransport), reporting "CROSS-TENANT DoS: tenant B was refused by a breaker that only tenant A's failures opened" and, on the chat path, "registry keys today are ['openrouter'] — they carry no tenant". The structural guard discovers 13 construction sites across 12 modules, and the boot census DERIVES 13 breaker-owning classes by enclosing ClassDef — both populations discovered, neither hand-typed. STUB-TREE PROOF (the red is behavioural, not existence-gated): a throwaway stub at the contracted module path — right class, right function, right signatures — whose `get_or_create` ignores its tenant key and returns ONE shared CircuitBreaker was placed in the tree and the suite re-run. Result: 14 failed, 1 passed, with ZERO AttributeError/TypeError — every failure an isolation assertion ('the registry returned the SAME CircuitBreaker for two different tenants', 'an unattributed call must never open a real tenant's breaker', 'registry exceeded its cap: 51'). The stub was then deleted; the tree is clean. Two checks were repaired because of this exercise: `test_breaker_still_protects_each_tenants_upstream` went GREEN against the stub (vacuous — it only asserted the module existed) and now asserts per-tenant breaker distinctness; `test_streaming_breaker_records_against_the_guarded_tenant` was failing on a ProviderKeyMissing setup artifact and now swaps ONLY the tenant contextvar between guard time and consume time, leaving the credential valid as production does. A FALSE RED was also caught and fixed: `test_tenant_a_trip_does_not_deny_tenant_b_on_chat_path` asserted a 200 return on a delegate that returned 500 for every model, which the wrapper re-raises — it could never have passed. Tenant B now calls a model the delegate serves, mapped to the SAME provider, so only a tenant dimension can separate them. Regression floor re-confirmed green at authoring time: tests/proxy/test_circuit_breaker_per_provider.py + tests/moderations_endpoint/test_cr1_per_tenant_breaker.py + tests/breaker_4xx_classification = 17 passed.
gate: pending — build COMPLETE, receipt 1 (exit 0), 18/18. Verified INDEPENDENTLY by the orchestrator, not relayed: the worker's own report never reached me, so every number below was re-run here.
- Suite: `tests/tenant_breaker_isolation` 18 passed. Siblings, three batches against a real DB (703 passed, 1 skipped, 0 failed): observability + cooldown_circuit + routing_admin + breaker_4xx_classification + model_fallbacks + retry_policy (138) · proxy + moderations_endpoint + guardrails + routing_strategy + vertex_upstream + streaming_resilience (164) · realtime* + usage* + azure_* + bedrock_* + openrouter_* + openai_* + error_aware_fallback + deployment_limits + balance_strategies + incremental_sse_streaming (401). `uv run pyright` = **0 errors, 0 warnings**. `ruff check` clean; `ruff format --check` 1523 files already formatted (CR-4 closed).
- THE GREEN IS NOT HOLLOW — proven by MUTATING THE FIX in three independent places and confirming the right guard went red each time, per [[guard-must-be-red-against-its-motivating-tree]] (a guard proven only on total absence is unproven against partial removal):
  (A) reversed the cooldown key order so `model_id` precedes the tenant segment -> `test_cooldown_key_cannot_be_collided_by_a_crafted_model_id` + `test_routing_admin_reports_an_honest_cross_partition_state` RED. R:KEY_COLLISION is earned.
  (B) collapsed `tenant_key_segment` to a constant so every tenant shares one bucket -> `test_unattributed_call_never_shares_a_bucket_with_a_real_tenant` + `test_cooldown_gate_is_tenant_partitioned` + `test_cooldown_key_cannot_be_collided_by_a_crafted_model_id` RED. R:SHARED_BUCKET is earned.
  (C) pinned `aggregate_breaker_state` to 0.0 — the exact D2 defect -> `test_breaker_state_gauge_reports_a_breaker_production_drives` RED.
  All three mutations reverted and verified byte-identical to their pre-mutation backups.
- CR-1 CONFIRMED: `_KNOWN_BREAKER_SITES` is gone; floors run over `_SYNTHETIC_VIOLATIONS` **and** `_SYNTHETIC_CLEAN` — both polarities, so a predicate that flagged everything would also fail. The one surviving `assert population` is over the synthetic corpus, not the live tree.
- CR-2 CONFIRMED: the boot census is `iter_live_breakers`, an `isinstance` walk over the live object graph with `id()`-keyed dedupe and `list()` snapshots (so it cannot raise "dict changed size" against a mutating registry). It plants a known set as its floor, asserts loose breakers are NOT mis-reported as owned, and — beyond what the contract asked — catches a breaker held only in a CLOSURE CELL, citing `main.py:1869` where the eval executor is wired exactly that way.
- COVERS-BINDING VERIFIED MECHANICALLY (this has cost three refreezes on R9): all 17 `## CHECKS` rows name a test that exists; every Must, every Reject and every EDGE is bound. The single unbound test is `test_structural_guard_predicate_is_not_vacuous`, which this node deliberately declares unbound.
residue found at verify by the orchestrator:
- CONCURRENCY, examined and CLEARED: the scrape-time walk cannot raise against a registry being mutated by live requests (`list()` snapshots, `id()` dedupe, bounded by `MAX_CENSUS_NODES = 200_000`, `except Exception -> 0.0` sentinel).
- UNPROVEN, deliberately NOT filed as a finding: `aggregate_breaker_state` never consults `scan.truncated`, so I suspected a truncated walk could report "closed" over an uninspected population — A18's forbidden shape on the metrics surface. **I could not reproduce it.** Two adversarial probes (an OPEN breaker behind `MAX_CENSUS_NODES + 5000` filler nodes; and the same buried four levels deep behind a 200k-wide shallow field) BOTH still reported 2.0 correctly, because the traversal reaches shallow app-state objects — where the registries actually live — before the budget runs out. Recorded as a theoretical concern with a cheap optional hardening (consult `truncated`), NOT as a defect. A claim I cannot demonstrate does not get filed as one.
residue declared at direction:
- DISCLOSED, red-first under an amendment: the CR-3 checks are authored AFTER the production fix already exists, so they cannot be red against the working tree the ordinary way — that would require un-fixing the code. They are instead proven red by the STUB-TREE method this task already used at direction: the shared registry / gate is swapped for a stub with the right module path, class, and signatures whose partitioning is a no-op (one shared breaker for every tenant; a key builder that drops the tenant segment), the four checks are run and must FAIL ON AN ISOLATION ASSERTION with zero AttributeError/TypeError, and the stub is then deleted. A check that stays green against that stub is vacuous and must be repaired before the gate — this is exactly how three defects were caught at direction, including one FALSE RED that could never have passed. The receipt records the stub run alongside the real one.
- BY DESIGN GREEN: `test_structural_guard_predicate_is_not_vacuous` is the only check that passes today. It is a property of the PREDICATE, not of the tree — it asserts ORIGINAL=True / MUTATED=False for every discovered population member (12/12 verified), so the guard cannot go vacuous the way the `catalog-sync-session-autobegin` census did. It is deliberately not bound by `covers:`.
- OPEN, decided by the human at freeze: SCOPE. Twelve adapter sites plus the wrapper, the cooldown port and keys, the admin read, the realtime path and the guard is a large single task. The isolation property does NOT hold unless the adapter layer AND the wrapper ship together (A21) — a partitioned wrapper over an unpartitioned adapter is a live DoS that also MASKS the defect in any test that drives only the outer layer. The cooldown gate (default-off) and the realtime path are the defensible cut lines.
- ADVISOR (appsec-engineer lens, propose-plan/refute) corrected this bundle on three points, each re-verified against the code before folding: (a) `ml_moderation_evaluator.py:151` is the registry's OWN construction, so the adapter count is ELEVEN not twelve — my first draft listed an already-fixed site as a defect; (b) A5's invariant is FALSE with a concrete falsifier at `cost_recovery.py:168`, now M11; (c) the static census is evadable, so a second boot-time census is required, now M12/S13. It also flagged an unbounded registry OUTSIDE this scope at `evals/runs/infrastructure/upstream_adapter.py:34` — noted below, not fixed here.
- OPEN, out of scope, no cross-tenant exposure claimed: `evals/runs/infrastructure/upstream_adapter.py:34` holds an UNBOUNDED breaker registry, and four seams already run their own per-tenant registries (`ml_moderation_evaluator.py:111`, `vector_stores/infrastructure/embedding_client.py:55`, `finetune/infrastructure/openai_client.py:69`, `mcp_connector/infrastructure/breaker_registry.py:28`). The promotion (M4) is what eventually collapses those four onto one implementation; this task does not chase them.
- OPEN, unverified: the M2 sweep is declared BY BREAKER ENTRY POINT, but I have not walked anthropic/vertex/gemini/bedrock/azure for `get_generation`-style secondary entries that reach `guard()` off the request path. Only the openrouter one is confirmed. That walk belongs to build.
- OPEN, decided at freeze: if Tin cuts Layer C (the default-off cooldown gate), it must leave behind a FAIL-LOUD boot guard that refuses to start with `cooldown_failure_threshold > 0` while the keys are still unpartitioned — otherwise enabling the feature silently reintroduces a cross-replica shared bucket.
- SUPERSEDED, was OPEN: A5's invariant — that an adapter with no tenant in context also has no credential and therefore never dials — is NOT proven. `_auth_headers` raises ProviderKeyMissing only INSIDE `_do_request`, i.e. AFTER `guard()` has already run (`openrouter_upstream.py:186-196` vs `:317`), so an unattributed call does reach the breaker. This is why A16's sentinel bucket is a required part of the contract rather than a defensive nicety.

## LESSONS
- pending — recorded at gate.
